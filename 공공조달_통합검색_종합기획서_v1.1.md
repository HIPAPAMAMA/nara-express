# 공공조달 통합검색 — 종합 기획 문서 (Claude Code 착수용)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.1 (계약정보·시장동향 통계 기능 추가) |
| 작성일 | 2026-09-04 |
| 성격 | 개인 프로젝트 — 비용 없음, 보안검토 없음, 로그인 없이 누구나 사용 |
| 기획 배경 | 타 부서 요청. AI뉴스룸 등 사내 정식 프로젝트와는 별개 |
| 근거 | 벤치마크 사이트([nara-search.vercel.app](https://nara-search.vercel.app)) DOM 분석 + PC 실행 화면 + 채팅 내 목업 확정(PC/모바일, 비비드 포인트) |
| 상태 | 디자인 확정 완료. API 실측(T-1~T-9, T-10) 완료. **1·2·3단계 핵심 기능 구현·실동작 검증 완료 (2026-09-05) — 통합검색·조회결과·상세첨부·대기업판정·청크엔진(진행률·중단·정직한 초과표시)·경쟁사·내 업체·쇼핑몰 상품검색** |

---

## 1. 개요

### 1.1 목적
나라장터(G2B) 공공조달 데이터를 검색·비교·추적하는 개인 프로젝트 웹앱을 개발한다. 공공데이터포털 오픈API를 데이터 소스로 하며, 입찰 실무자가 낙찰결과·입찰공고·사전규격을 한 화면에서 확인하고 경쟁사 실적을 비교할 수 있도록 한다. 회사 예산·보안검토 없이 개인 계정(공공데이터포털·Firebase)만으로 진행한다.

### 1.2 대상 사용자
- 1차: 입찰·제안 실무 담당자 (요청 부서, 소수)
- 2차: 사업기획·영업 관리자

### 1.3 문서 범위
- 포함: 화면 구성, 기능 정의, API 매핑, 데이터 모델, 공통 규칙, 디자인 시스템, 단계별 개발 범위
- 제외: 실제 코드, 인프라 구성 스크립트 (Claude Code에서 진행)

### 1.4 용어

| 용어 | 정의 |
|---|---|
| 청크(Chunk) | 오픈API 날짜 범위 제한을 우회하기 위해 조회 기간을 잘게 나눈 단위 |
| 빠른 조회 | 최근 구간 청크만 조회. 속도 우선, 누락 가능 |
| 정밀 조회 | 선택 기간 전체 청크 조회. **(2026-09-05 정정) "100% 완전 수집"이 아니라 "선택 기간 전체를 훑되 청크당 999건 상한까지만" — 초과분은 9장 T-10 참고, 정직하게 노출** |
| 낙찰률 | 추정가격(또는 기초금액) 대비 낙찰금액 비율(%) |

---
## 2. 화면 목록

| 화면ID | 화면명 | 유형 | 비고 |
|---|---|---|---|
| SC-01 | 통합검색 | 워크스페이스 | 진입 기본 화면 |
| SC-02 | 조회 결과 | 워크스페이스 | 검색·경쟁사분석 결과 공용 |
| SC-03 | 평가분석 | 워크스페이스 | 5단계 (보류) |
| SC-04 | 관심 키워드 | 워크스페이스 | |
| SC-05 | 경쟁사 | 워크스페이스 | |
| SC-06 | 저장내역 | 워크스페이스 | |
| SC-07 | 알림·설정 | 워크스페이스 | 4단계 |
| SC-08 | 공고 상세·첨부 | 패널/모달 | SC-02에서 호출 (계약정보 포함) |
| SC-09 | 평가점수 | 패널/모달 | 5단계 (보류) |
| SC-10 | ~~시장동향 통계~~ | 워크스페이스 | **보류 (2026-09-07)** — 공공조달통계정보서비스에 실제 데이터 없음 확인, 3.7절 근거 참고 |
| SC-11 | 내 업체 | 워크스페이스 | 신규 — 사용자정보서비스(`getPrcrmntCorpBasicInfo02`) 기반, 공고 적합도 판정 |
| SC-12 | 쇼핑몰 상품검색 | 워크스페이스 | 신규 — 종합쇼핑몰 품목정보서비스(`ShoppingMallPrdctInfoService`) 활용신청·실측 완료 |
| SC-13 | ~~대시보드~~ | 워크스페이스 | **신규 (2026-09-08)** — 상세는 3.10절 참고 |

**네비게이션 — 대메뉴 3개로 최종 정리 (2026-09-08)**
- 데스크톱·모바일 공통: **통합검색 / 대시보드 / 내설정** 3개 대메뉴만 상단(모바일은 하단)에 노출
  - **통합검색**: 진입 기본 화면(원래대로 복귀 — 한때 대시보드를 기본 화면으로 시도했다가 되돌림)
  - **대시보드**: 등록 현황(관심 키워드·경쟁사·내 업체 수) 요약 + 메뉴 카드로 관심 키워드·경쟁사·쇼핑몰 상품검색·저장내역·평가분석 전체를 흡수 — 이 5개 화면은 더 이상 대메뉴가 아니라 대시보드 안에서만 진입
  - **내설정**(신규 대메뉴): 내 업체(SC-11) + 알림·설정(SC-07)을 내부 탭 2개로 묶음 — 둘 다 "한 번 등록·설정하고 마는" 성격이 같다는 이유로 통합
- 우상단 상시: API 연결 상태 표시

> **메뉴 구조 변천사**: ① 최초엔 9탭 전부 상단에 나열 → ② 모바일 5탭 한계로 "더보기" 시트 도입(2026-09-07, 아래 첫 번째 정정 참고) → ③ 대시보드 신설 후 한때 그것을 기본 화면으로 삼고 나머지를 "더보기"처럼 흡수(2026-09-08 오전) → ④ **통합검색을 다시 기본 화면으로 되돌리고, 대시보드·내설정 2개만 흡수 대메뉴로 남겨 최종 3개 구조로 정리**(2026-09-08 오후, 최종). "더보기" 바텀시트는 더 이상 필요 없어져 완전히 제거함(`MobileMoreSheet.jsx` 삭제).
> **정정 (2026-09-07, 과거 기록)**: 처음엔 5탭에 "설정"까지 넣고 "더보기"는 별도 진입 경로로 남겨뒀는데, 실제 구현 단계에서 5탭 슬롯에 "더보기"까지 넣을 자리가 없다는 게 드러났다(검색·경쟁사·키워드·저장내역 4개 + 설정 + 더보기 = 6개, 하단 탭은 5개가 한계). 그래서 "설정"을 더보기 시트 안으로 옮기고, 그 자리를 "더보기" 탭으로 바꿨다 — 내 업체·쇼핑몰·설정·평가분석 4개 전부 더보기 시트에서 진입. 이 작업 전까진 모바일에서 내 업체·쇼핑몰 화면에 아예 갈 수 없었던 버그이기도 했다. **(위 최종 구조로 대체됨 — 더보기 시트 자체가 없어짐)**

> **벤치마크(nara-search.vercel.app) 대비**: 스타일(7장 크림+클레이 팔레트)은 독자 확정이라 그대로 유지하고, "내 업체"·"쇼핑몰 상품검색" **두 화면은 기능 자체를 벤치마크해 처음부터 포함**한다(2026-09-05 확정). 상세 기능은 3.8·3.9절 참고.

---

## 3. 기능 정의

### 3.1 SC-01 통합검색

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| SRC-001 | 데이터 유형 선택 | 낙찰결과 / 입찰공고 / 사전규격 (단일 선택) | 1 |
| SRC-002 | 검색어 입력 | ① 사업명·공고번호 ② 업체명 (2개 필드 분리) | 1 |
| SRC-003 | 기간 직접 입력 | 시작일 ~ 종료일 | 1 |
| SRC-004 | ~~기간 프리셋~~ | **6종으로 축소 (2026-09-08)** — 1·3·6개월 / 1·3·5년. 9개월·2년·4년은 실사용 빈도가 낮다는 피드백으로 제거 | 1 |
| SRC-005 | 조회 방식 선택 | 빠른 조회 / 정밀 조회 | 2 |
| SRC-006 | 조회 실행 | 조회하기 | 1 |
| SRC-007 | 조회 중단 | 진행 중 요청 취소 (AbortController) | 2 |
| SRC-008 | ~~최근 검색어~~ | **제거 (2026-09-08)** — 조회하기 버튼 아래 "bid·2026-08-08~2026-09-07" 식으로 뜨는 게 혼란스럽다는 피드백으로 기능 자체를 삭제(관련 저장 로직도 함께 정리) | 1 |
| SRC-009 | 관심 키워드 연동 | 등록 키워드 노출, 클릭 시 검색어 입력 | 3 |
| SRC-010 | ~~재공고 레이더~~ | **구현·확인 완료 (2026-09-08)** — 작년 이맘때 낙찰 이력을 기존 낙찰정보서비스로 조회해 1년 주기 재공고 시점을 추정(D-day, 현직 업체·낙찰률 표시). 새 API 없이 기존 데이터만 재사용, 통합검색 사이드바에 위젯으로 내장(별도 탭 아님) — 상세는 8장 5단계 근거 참고 | 5 |
| SRC-019 | ~~발주계획 레이더~~ | **구현·확인 완료 (2026-09-08)** — 나라장터 발주계획현황서비스(신규 API, 별도 신청 필요)로 앞으로 6개월 이내 공시된 발주계획에서 키워드 검색. 이미 실제 공고로 이어졌으면 "공고 게시됨" 배지 표시. 상세는 8장 5단계 근거 참고 | 5 |
| SRC-011 | 상세조건 — 업무구분 | 전체/용역/물품/공사/외자/기타 | 1 |
| SRC-012 | 상세조건 — 공고태그 | | 3 |
| SRC-013 | 상세조건 — 발주기관 | | 1 |
| SRC-014 | 상세조건 — 수요기관 | | 1 |
| SRC-015 | 상세조건 — 지역 | | 1 |
| SRC-016 | 상세조건 — 업종 | 업종코드 | 3 |
| SRC-017 | 상세조건 — 추정가격 | 하한 ~ 상한 | 1 |
| SRC-018 | API 상태 확인 | 진입 시 health check, 결과 상시 표시 | 1 |

> **SRC-011 주의**: 업무구분은 단순 필터가 아니라 **호출 엔드포인트를 바꾸는 스위치**다. 오픈API가 업무구분별 오퍼레이션으로 분리되어 있어, 구분에 맞는 오퍼레이션을 써야 정상 응답이 온다. "전체" 선택 시 4개 오퍼레이션을 병렬 호출 후 병합해야 한다.

### 3.2 SC-02 조회 결과

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| RES-001 | 결과 목록 | 4열: 공고·사업명 / 업체·기관 / 금액·일정 / 핵심지표 | 1 |
| RES-002 | 조회 요약 배너 | 완료 문구 + 건수 + 청크 진행률 + 소요시간 | 2 |
| RES-003 | 범위 경고 배너 | 빠른 조회 시 "정밀 조회 선택" 안내 (앰버) | 2 |
| RES-004 | 결과 내 검색 | 클라이언트 필터 | 1 |
| RES-005 | 유형 필터 | 전체/낙찰결과/입찰공고/사전규격/유찰·재입찰만 | 1 |
| RES-006 | 정렬 | 기본/금액높은순/금액낮은순/마감임박순/최신순 | 1 |
| RES-007 | 보기 밀도 | 간단 / 상세 | 1 |
| RES-008 | 더 보기 | 30건 단위 추가 로드 | 1 |
| RES-009 | 맨 위로 | | 1 |
| RES-010 | 내보내기 — CSV | | 1 |
| RES-011 | 내보내기 — Excel | | 1 |
| RES-012 | 내보내기 — 복사 | 클립보드 | 1 |
| RES-013 | 내보내기 — 공유 | Web Share API | 3 |
| RES-014 | 일괄 — 제안서 | 결과 전체의 제안·규격서 첨부 일괄 처리 | 3 |
| RES-015 | 일괄 — 공고 | 결과 전체 공고 일괄 처리 | 3 |
| RES-016 | 공고번호 복사 | 카드 내 복사 아이콘 | 1 |
| RES-017 | 나라장터 원문 열기 | 새 창 | 1 |
| RES-018 | ~~낙찰 알림 등록~~ | **구현·확인 완료 (2026-09-07)** — 공고 상세의 "낙찰 알림 등록" 버튼(입찰공고에만 노출). 하루 1회 크론이 낙찰결과를 조회해 등록된 공고번호가 나오면 카카오톡 발송 후 추적목록에서 자동 제거 | 4 |
| RES-019 | 상세·첨부 열기 | SC-08 호출 | 1 |
| RES-020 | 평가점수 열기 | SC-09 호출 | 5 |
| RES-021 | 사이드 요약 패널 | 검색건수 / 조회유형 / 조회기간 / 저장항목 수 | 1 |
| RES-022 | 요약 코멘트 카드 | 결과 성격에 따른 한 줄 요약 (마젠타 박스) | 3 |
| RES-023 | 경쟁사 합계 카드 | 총 낙찰금액 + 업체별 건수/금액 + 비율 바 | 3 |
| RES-024 | 빈 상태 | 일러스트 + 안내 문구 | 1 |

**카드 필드 — 낙찰결과 / 경쟁사 분석**
```
순번 · 사업명 · 공고번호 + 공고일 + 복사버튼
업체명 + 사업자번호
낙찰금액(원) + 억 환산 · 예산(추정가격)
[낙찰] 뱃지 · 낙찰률 · [평가점수] 버튼
```

**카드 필드 — 입찰공고**
```
순번 · 사업명 · 공고번호 + 등록일시(초단위) + 복사버튼
발주기관 / 수요기관
입찰마감 일시 + D-n 뱃지(또는 "마감") · 개찰 일시
[공고] 뱃지 · 나라장터 원문 열기 · [낙찰 알림] [상세·첨부]
```

### 3.3 SC-05 경쟁사

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| CMP-001 | ~~사업자번호 직접 등록~~ | **구현·확인 완료** — localStorage(`competitors`) 저장 | 3 |
| CMP-002 | ~~업체명 자동 매칭~~ | **구현·확인 완료 (2026-09-05)** — 업체명 입력 → 후보 칩 목록 제시. 사용자정보서비스에는 정방향 검색이 없어(T-5), 낙찰정보서비스에 최근 1년치 청크 잡(kind=award, mode=full, keywordType=company)을 실시간으로 돌려 `bidwinnrNm`+`bidwinnrBizno` 일치 건을 찾는 방식으로 구현. 실제 업체명("크리에이텍")으로 후보 칩 노출까지 확인 | 3 |
| CMP-003 | ~~후보 칩 표시~~ | **구현·확인 완료** — 업체명 / 사업자번호 (법인등록정보는 데이터 소스에 없어 제외) | 3 |
| CMP-004 | ~~후보 선택 등록~~ | **구현·확인 완료** — 칩 클릭 시 즉시 등록 + 토스트 | 3 |
| CMP-005 | ~~등록 목록~~ | **구현·확인 완료** — 체크박스 + 업체명 + 사업자번호 | 3 |
| CMP-006 | ~~개별 분석 / 삭제~~ | **구현·확인 완료** — 행 단위 액션 | 3 |
| CMP-007 | ~~다중 선택 액션~~ | **구현·확인 완료** — 선택 분석 / 선택 삭제 | 3 |
| CMP-008 | ~~전체 삭제~~ | **구현·확인 완료** — 확인 다이얼로그 필수 | 3 |
| CMP-009 | ~~등록 카운트 뱃지~~ | **구현·확인 완료** — 헤더 우측 "n개" | 3 |
| CMP-010 | ~~분석 실행~~ | **구현·확인 완료 — 단, SC-02가 아니라 SC-05 화면 안에 인라인 렌더링**(별도 화면 없음 원칙은 동일하게 지킴). 최근 2년치 낙찰 이력을 실제로 조회해 총 낙찰금액·건수·목록 표시까지 확인 | 3 |
| CMP-011 | ~~빈 상태~~ | **구현 완료** — "업체명만 입력해도 자동 등록" 안내 문구(일러스트는 생략) | 3 |

### 3.4 SC-04 관심 키워드 / SC-06 저장내역 / SC-07 알림·설정

> **UI 배치 정정 (2026-09-08)**: SC-07(알림·설정)은 더 이상 독립 대메뉴가 아니다. SC-11(내 업체)과 함께 "내설정"이라는 하나의 대메뉴로 묶여 내부 탭 2개(내 업체 / 알림·설정)로 진입한다 — 기능·기능ID(SET-001~008)는 그대로, 화면 껍데기(`MySettingsWorkspace.jsx`)만 새로 생긴 것. 상세는 2장 네비게이션·3.10절 참고.

> 벤치마크 사이트에서 내부 화면 미확인. 아래는 연결 지점 기반 정의이며 3단계 이후 상세화한다.

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| KWD-001 | ~~키워드 등록/삭제~~ | **구현·확인 완료 (2026-09-06)** — localStorage(`keywords`), 카카오 연결 시 서버(KV)에도 동기화 | 3 |
| KWD-002 | ~~키워드 → 검색 실행~~ | **구현·확인 완료** — 클릭 시 SC-01로 전환되며 최근 31일·빠른조회로 자동 실행까지 확인 | 3 |
| KWD-003 | ~~키워드 알림 설정~~ | **구현·확인 완료 (2026-09-07)** — 관심 키워드 칩의 🔔 아이콘으로 on/off. 하루 1회 크론이 최근 2일치 입찰공고를 조회해 키워드 매칭되면 카카오톡 발송 | 4 |
| SAV-001 | ~~공고 저장/해제~~ | **구현·확인 완료** — 결과 목록 행의 ☆ 아이콘, localStorage(`savedItems`), 카카오 연결 시 서버(KV)에도 동기화 | 3 |
| SAV-002 | ~~저장 목록 조회~~ | **구현·확인 완료** | 3 |
| SAV-003 | ~~첨부파일 보관~~ | **구현·확인 완료 (2026-09-08)** — 실제 파일을 복사·보관하는 대신 원본 링크만 저장(사용자 선택). 이미 있는 SAV-001 저장 인프라를 재사용해 item 전체(첨부파일 링크 포함)가 저장내역에 담기므로 새 저장소 없이 해결 | 3 |

> **관심 키워드·저장내역 기기 간 동기화 (2026-09-07 추가)**: 원래 둘 다 브라우저 localStorage에만 저장돼서 "휴대폰에서 저장한 걸 PC에서 이어보기"가 안 됐다. 새 회원가입 시스템을 만들지 않고, 이미 있는 카카오 로그인(uid)을 재사용해 Vercel KV에도 동일 목록을 저장하는 방식으로 해결(`functions/src/lib/userDataStore.js`, `/api/userdata/keywords`·`/api/userdata/saved`). 카카오 연결 시 로컬(비로그인 때 쌓인 것)과 서버 데이터를 합집합으로 병합해 양쪽에 반영하고, 이후 추가/삭제는 즉시 서버에도 반영한다(`web/src/lib/cloudSync.js`). 비로그인 사용자는 기존과 동일하게 localStorage만 사용 — 동작 변화 없음.
> **알려진 한계**: 삭제는 union-merge 특성상 완전히 동기화되지 않을 수 있다 — 기기 A가 오프라인인 동안 기기 B에서 항목을 지우면, A가 나중에 온라인이 될 때 그 항목이 되살아날 수 있음(타임스탬프 기반 tombstone 없음). 개인용 소규모 목록 도구 수준에선 감수 가능하다고 판단, v1 범위에서는 별도 처리 안 함.
| SET-001 | ~~알림 수신 설정~~ | **구현·확인 완료 (2026-09-07)** — 알림·설정 화면에서 전체 on/off (채널·빈도별 세분화는 v1 범위 밖) | 4 |
| SET-002 | ~~낙찰 알림 목록~~ | **구현·확인 완료 (2026-09-07)** — RES-018 등록분을 알림·설정 화면에서 목록으로 보고 개별 삭제 가능 | 4 |
| SET-003 | ~~데이터 초기화~~ | **구현·확인 완료 (2026-09-08)** — 내 업체·관심 키워드·경쟁사·저장내역을 이 기기에서 전부 삭제. 카카오 연결 중이면 동기화된 서버 사본도 함께 비움. 카카오 연결 자체는 유지 | 3 |
| SET-004 | ~~카카오톡 나에게 보내기 연동~~ | **구현·실사용 확인 완료 (2026-09-07)** — 상세 설계는 아래 근거 참고 | 4 |
| SET-005 | 자동 점검 범위 설정 | 낙찰결과(개찰)/입찰공고(본공고)/사전규격 개별 on-off — v1은 전체 하나로 통합, 세분화는 범위 밖 | 4 |
| SET-006 | ~~중복 발송 방지~~ | **구현·확인 완료 (2026-09-07)** — 발송 성공한 것만 이력에 남기고, 실패하면 기록을 안 남겨서 다음날 자동 재시도(별도 재시도 큐 없이) | 4 |
| SET-007 | ~~데이터 백업~~ | **구현·확인 완료 (2026-09-08, 신규)** — 내 업체·키워드·경쟁사·저장내역을 JSON 파일로 내보내기·가져오기. 카카오 로그인 없이 기기를 옮길 수 있는, 로그인 불필요한 보완재 | 3 |
| SET-008 | ~~홈 화면에 추가(PWA)~~ | **구현·확인 완료 (2026-09-08, 신규)** — manifest.webmanifest + 통과용 서비스워커(오프라인 캐싱 없음, 설치 조건 충족 목적)로 앱처럼 설치 가능. beforeinstallprompt 지원 브라우저는 버튼으로, iOS는 공유 메뉴 안내로 폴백 | 3 |

> **SET-004 상세**: 카카오 알림톡(비즈니스 채널·딜러사·템플릿 심사)이 아니라 **카카오톡 메시지 API "나에게 보내기"**. 카카오 개발자 앱 등록(무료·즉시) + 카카오 로그인 + 동의항목(카카오톡 메시지 전송) 구성만으로 가능. 발송대행사·사업자 인증·건당 과금 불필요.
> 단, **팀 단위 서비스라 사용자별 개별 연결이 필요**하다 — 각 사용자가 자기 카카오 계정을 연결해야 하며, 서버는 사용자별 access token + refresh token을 저장·자동 갱신한다.
>
> **구현 근거 (2026-09-07)**: 원래 이 문서는 저장소로 Firestore를 전제했으나, 그사이 Vercel로 완전히 전환됐으므로 **이미 쓰고 있는 Vercel KV를 그대로 재사용**했다(`functions/src/lib/alertStore.js`) — 새 DB 추가 없음. 예상 사용자 수(~50명)를 감안해 카카오 공식 문서를 직접 확인한 결과, "나에게 보내기"는 **친구 메시지와 달리 발송 건수 제한이 없고 앱 심사·팀원 등록 여부와 무관하게 로그인한 누구나 즉시 사용 가능**하다는 걸 확인해 규모 문제는 해소됨. 정부 API 호출량이 사용자·키워드 수와 무관하게 하루 상수(≤8콜)로 고정되도록, 사용자별/키워드별로 따로 조회하지 않고 날짜범위 1회 조회 후 전체 구독자에 대해 클라이언트에서 매칭하는 방식으로 설계했다(`functions/src/lib/alertRunner.js`). 세션은 별도 저장소 없이 서명 쿠키(HMAC) 하나로 처리 — 모든 데이터가 카카오 user id로 KV에 저장되니 쿠키의 uid 자체가 조회키다. Vercel Hobby 플랜은 크론이 하루 1회 제한이라 알림도 하루 1회 배치로 동작(실시간 아님).
> **카카오 토큰 정책 실측 (T-8 해소, 2026-09-07)**: 액세스 토큰 만료 약 12시간, 리프레시 토큰 만료 약 60일. 리프레시 토큰은 매번 갱신되지 않고 남은 유효기간이 1개월 미만일 때만 새로 발급된다 — 응답에 없으면 기존 값을 유지해야 함.
> **콘솔·환경변수 설정 완료 (2026-09-07)**: 카카오 개발자 콘솔 앱 등록·Redirect URI 등록·동의항목 설정, Vercel 환경변수(`KAKAO_CLIENT_ID`·`KAKAO_CLIENT_SECRET`·`KAKAO_REDIRECT_URI`·`SESSION_SECRET`·`CRON_SECRET`) 추가까지 사용자가 직접 완료 — 실제 배포 사이트에서 카카오 로그인 성공 확인.
> **버그 수정 (2026-09-07, 닉네임 미표시)**: 로그인 후 "OOO님 연결됨"에 닉네임이 안 뜨고 "님 연결됨"만 표시되던 문제. 원인 2가지 — ① `/v2/user/me` 호출 시 `property_keys=["kakao_account.profile"]`로 필터링했는데 이건 유효한 리프 키가 아니라(정확히는 `kakao_account.profile.nickname`) `kakao_account` 전체가 응답에서 빠짐, ② 더 근본적으로 로그인 요청(authorize URL)의 `scope`에 `talk_message`만 넣고 `profile_nickname`을 빠뜨려서 애초에 닉네임 동의 자체가 요청되지 않음(콘솔에서 동의항목을 켜둬도 실제 로그인 요청에 넣지 않으면 동의창에 뜨지 않고 권한도 안 내려옴). `property_keys` 파라미터 제거 + `scope=talk_message,profile_nickname`으로 수정, 재연결 후 정상 표시 확인.

### 3.5 SC-08 공고 상세·첨부

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| DTL-001 | 입찰진행정보 | 공고일 / 제안서마감 / 입찰마감 / 개찰일 | 1 |
| DTL-002 | 입찰자격·제한 | 자격 / 보증 / 공동수급 / 지역·면허 제한 | 3 |
| DTL-003 | 첨부파일 목록 | 공개 첨부 전체 | 1 |
| DTL-004 | 제안·규격서 우선 정렬 | 제안요청서·과업지시서·규격서를 상단 고정 | 3 |
| DTL-005 | 전체 저장 | 첨부 일괄 다운로드 | 3 |
| DTL-006 | 액션 4종 + RES-018 | 공고번호 복사 / 낙찰결과 조회 / 제안·규격서 저장 / 원문 열기 + 낙찰 알림 등록(입찰공고에만, 4단계) | 1 |
| DTL-007 | ~~계약체결정보 표시~~ | **보류 (2026-09-07)** — 계약일자 / 확정계약금액 / 계약상대자, 낙찰 건에 한해 노출 예정이었으나 아래 실측 결과 현재 방식으론 구현 불가 판정 | 3 |
| DTL-008 | 계약변경이력 | 계약금액·기간 변경 이력 목록 — DTL-007과 같은 이유로 착수 안 함 | 3 |
| DTL-009 | **대기업 참여제한 자동 판정** | DTL-004가 우선정렬해둔 **제안요청서·과업지시서·규격서류만**(입찰유의서·계약일반조건 등 표준 서식 첨부는 제외) 다운로드→텍스트 추출→"대기업", "참여제한", "중소기업자간 경쟁제품", "소프트웨어사업 대기업 참여 제한" 등 키워드 매칭. 결과로 ① 뱃지(**제한있음 / 제한없음 / 확인필요**) ② 매칭된 문구 발췌(어느 파일 몇 번째 문장인지) ③ 해당 첨부파일 원문 바로가기 링크, 이 세 가지를 함께 표시. 최종 판단은 반드시 원문에서 하라는 면책 문구 필수 | 1 |
| DTL-010 | 목록 카드 "대기업 제한 확인" 버튼 | RES-001 목록 카드에 버튼 노출, **클릭 시에만** 그 건 하나만 DTL-009 판정 실행 (자동 전수 판정 아님 — 정밀 조회로 수백 건이 나와도 클릭 안 하면 처리 안 함). 판정 결과는 캐시해서 같은 건 재클릭 시 즉시 표시 | 1 |

> **DTL-007·008 근거 (당초 설계, 폐기됨)**: 나라장터 계약정보서비스(활용신청 완료)로 구현. 낙찰정보만으로는 "낙찰됐다"까지만 알 수 있고, 실제 계약체결 여부·금액 확정·변경 이력은 이 API가 있어야 확인된다. 낙찰 건 상세를 열었을 때만 추가 호출(지연 로딩)하여 API 트래픽을 아낀다.
>
> **보류 사유 — 실측 결과 (2026-09-07)**: 착수 전 API 오퍼레이션을 직접 두드려본 결과, 이 서비스는 award/bid처럼 **공고번호 단건 조회가 안 된다** — `bidNtceNo`+`bidNtceOrd`를 넘기면 `HTTP_ERROR`. 실제로는 award/bid와 같은 **날짜범위 대량조회** API(`inqryDiv`·`inqryBgnDt`·`inqryEndDt`, 업무구분별 `getCntrctInfoList{Thng|Servc|Cnstwk|Frgcpt}`)라서, 특정 공고의 계약정보를 보려면 "그 공고 근처 날짜로 대량조회 → 응답의 `ntceNo` 필드로 클라이언트가 직접 대조"하는 수밖에 없다. 문제는 **하루치 물량만도 업무구분 하나에 수천 건**(2026-09-07 실측: 물품 1,528건·용역 3,278건, 단 하루)이라 한 페이지(999건 상한)로는 원하는 건이 잡힐 확률이 매우 낮고, 못 찾았다고 "계약정보 없음"이라 보여주면 실제로는 있는데 못 찾은 걸 "없다"고 오인시키는 셈이라 6.3 원칙(정직하게 표시)에 어긋난다. 완전 수집을 시도하면 detail 패널 한 번 열 때마다 수십 콜이 필요해 일일 1,000건 한도를 감당 못 한다(T-10과 같은 종류의 물리적 한계). `ntceNo`는 경쟁입찰(제한경쟁·일반경쟁) 낙찰 건에만 채워지고 수의계약 건은 애초에 공고가 없어 비어 있다는 것도 확인함 — 이 자체는 문제 없지만 위 볼륨 문제 때문에 기능 자체를 구현하지 않기로 했다. 날짜범위 상한은 이 서비스만 특이하게 **31일 고정**(32일부터 resultCode 07 에러 — award/bid의 '+1개월 달력' 규칙과 다름)이라는 것도 확인해뒀다, 혹시 나중에 다른 접근(예: 공공데이터포털에 별도 문의해 단건조회 파라미터를 확인하는 등)으로 재시도할 경우 참고할 것.
>
> **DTL-009·010 근거 (2026-09-05 추가, 실사용자 핵심 요구)**: 사용자가 대기업 소속이라 참여제한이 걸린 공고를 매번 첨부파일을 직접 열어 확인해야 하는 반복 작업이 있음 — 이를 자동화하는 게 이 프로젝트의 실질적 존재 이유 중 하나. 입찰공고정보서비스 API 필드(`indstrytyLmtYn`·`prdctClsfcLmtYn`·`bidPrtcptLmtYn` 등) 어디에도 "대기업참여제한" 명시 필드가 없음을 확인함 — 이 정보는 구조화 데이터가 아니라 **첨부파일 본문에 문구로만 존재**하므로, 파일을 열어 텍스트를 읽는 방식 외엔 API로 직접 얻을 방법이 없다.
> **기술 스택 메모**: 첨부파일 URL(`ntceSpecDocUrl1~10`)은 T-6에서 인증 없이 직접 다운로드 가능 확인 완료(공공데이터포털 API 트래픽과 무관 — g2b.go.kr 자체 다운로드라 일일 1,000건 한도 안 씀). **실측 완료 (2026-09-05, 최근 5일 입찰공고 30건 샘플)**: 첨부 있는 26건에서 파일 85개 확인 — **PDF 35개(41%) vs HWP 26개(31%) + HWPX 20개(24%)**. HWP 계열이 절반 가까이라 PDF만 처리해서는 부족함이 확정적으로 확인됨. `ntceSpecDocUrl`은 다운로드 서블릿 URL이라 확장자가 없고, 실제 확장자는 `ntceSpecFileNm`(파일명) 필드로 판단해야 함.
> - **HWPX**(신형, 2010 이후 규격)는 ZIP 컨테이너 안에 XML이 들어있는 구조라 별도 외부 변환 없이 자체 파서로 텍스트 추출 가능 — 20개(24%)는 상대적으로 쉬움
> - **HWP**(구형, 바이너리)는 LibreOffice headless 변환(`soffice --headless --convert-to txt`) 등 외부 변환기가 사실상 필수라고 봤음 — **Cloud Functions 표준 런타임에서 바로 못 돌아서 Cloud Run(컨테이너) 인프라를 추가하기로 확정**(2026-09-05, 12장 인프라 참고). PDF/HWPX는 Cloud Functions에서 직접 처리, HWP만 Cloud Run으로 라우팅하려 했음.
> **정정 (2026-09-07)**: Vercel 전환 이후 재검토한 결과 `@ohah/hwpjs`(Rust 네이티브 파서) npm 패키지로 Cloud Run 없이도 HWP 텍스트 추출이 가능함을 확인 — 위 계획은 폐기, 아래 8장·12장 참고.
> **오탐 방지**: 키워드 매칭은 완벽하지 않으므로(예외 조항·조건부 제한 등 뉘앙스 존재) "확인필요" 뱃지와 원문 링크를 항상 병기하고, 절대 "제한없음"을 100% 확정처럼 노출하지 않는다.
> **자동 전수 판정 대신 온디맨드로 전환 (2026-09-05 결정)**: 목록에 결과가 수백 건 나와도 사용자가 실제로 관심 있는 건만 클릭해서 확인하는 방식이 합리적 — 비용(Cloud Run은 처리한 만큼만 과금, 개인 사용량이면 무료 한도 내)보다는 **불필요한 처리량·속도**가 진짜 이유. 정밀 조회로 264건이 나와도 백그라운드로 264번 HWP 변환을 시도하지 않는다.

### 3.6 SC-03 · SC-09 평가분석 (5단계, 보류)

| 기능ID | 기능명 | 상세 |
|---|---|---|
| EVL-001 | 평가위원별 합계 | 위원별 총점 + 1위 대비 차이 |
| EVL-002 | 분야별 배점표 | 분야 / 배점 / 위원별 원점수 |
| EVL-003 | 1위 대비 분석 | 가격·기술·종합 점수차, 위원별 판세 |
| EVL-004 | Excel 저장 | |
| EVL-005 | 폴백 안내 | 팝업·세션 제한 시 원문 확인 경로 안내 |

> **보류 사유**: 평가점수는 오픈API 미제공 항목으로, 나라장터 팝업 페이지 파싱이 필요하다. ① 나라장터 이용약관상 자동수집 가부 ② 평가위원 실명 노출에 따른 개인정보 취급 ③ 사내 보안검토 통과 여부 — 세 가지 확인 전에는 착수하지 않는다.
> 대안: 1~4단계는 오픈API만으로 완결되며, 평가점수는 "나라장터 원문 열기" 링크로 대체해도 실무 가치 손실이 크지 않다.
> **추가 실측 (2026-09-07)**: 마스킹("김**")으로 개인정보 문제를 완화할 수 있는지 검토하던 중, 더 근본적인 문제를 확인함 — g2b.go.kr 공고 상세페이지는 로그인 없이도 공개(비로그인 시 담당자 성명은 사이트가 자체적으로 "성+**"로 마스킹)이지만, **평가위원별 점수 자체는 공고마다 일정한 위치에 구조화되어 있지 않다.** "제안서 평가결과 공개" 같은 게시물이 존재하긴 하나 이는 발주기관이 재량으로 올리는 "기관별 공지사항" 중 하나일 뿐 — 공고번호로 조회하면 항상 나오는 데이터가 아니고(기관마다 공개 여부가 다름), 공개하더라도 표가 아니라 첨부파일(PDF·HWP)일 가능성이 높아 자동 파싱 대상이 못 됨. 즉 이용약관·개인정보 문제 이전에 **"모든 공고에 대해 동작하는 기능"으로 만들 수 있는 데이터 구조 자체가 아님** — DTL-007·008, SC-10과 같은 종류의 구조적 한계. 마스킹 여부와 무관하게 보류 유지 결정.

### 3.7 SC-10 시장동향 통계 (신규) — 보류 (2026-09-07)

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| STA-001 | ~~발주기관별 발주 규모~~ | **보류** — 연도·기관 선택 시 총 발주금액·건수 | 4 |
| STA-002 | ~~연도별 추이 차트~~ | **보류** — 최근 5년 발주 규모 라인차트 | 4 |
| STA-003 | ~~업종·업무구분별 비중~~ | **보류** — 물품/용역/공사/외자 비율 | 4 |
| STA-004 | ~~경쟁사 화면 연동~~ | **보류** — 등록된 경쟁사의 소속 업종·발주기관 통계와 교차 표시 | 4 |

> **보류 사유 (2026-09-07 실측)**: 착수 전 공공조달통계정보서비스를 직접 두드려봤다. 계약정보서비스와 달리 이번엔 사용자가 data.go.kr 활용신청 상세 > 상세기능 탭을 캡처해줘서 정확한 요청변수를 바로 확인함 — 오퍼레이션 `getTotlPubPrcrmntSttus`, 필수 파라미터 `srchBssYear`(검색기준년도). 그런데 문서의 샘플값(`2016`)을 포함해 2016~2026 사이 여러 연도로 조회해도 전부 `resultCode: 00`(정상) + `totalCount: 0`으로 응답이 옴 — 파라미터가 틀린 게 아니라 **API 자체에 실제 데이터가 없는 상태**다. 사용자가 data.go.kr의 자체 "미리보기" 도구로도 동일하게 `totalCount: 0`을 확인해줘서, 이게 우리 쪽 호출 방식 문제가 아니라 이 오퍼레이션이 등록만 되어 있고 데이터가 비어있는(또는 더 이상 갱신 안 되는) 상태임이 확정됐다. 오퍼레이션명·파라미터명은 옳게 확인해뒀으니, 나중에 조달청이 이 데이터를 다시 채우거나 다른 오퍼레이션(상세기능 탭에 `getTotlPubPrcrmntSttus` 외 다른 항목이 있는지는 미확인 — 목록 상단만 캡처됨)이 있는지 재확인이 필요하면 이 근거부터 참고할 것.

> **근거**: 공공조달통계정보서비스(활용신청 완료)로 구현. 집계 API라 청크 분할 없이 단건 호출로 끝나 2단계 엔진과 무관 — 구현 난이도는 낮음. 다만 화면 자체(차트 3종)가 신규라 4단계에 배치.

### 3.8 SC-11 내 업체 (신규, 벤치마크 기능 이식)

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| MYC-001 | ~~내 업체 정보 자동 입력~~ | **구현·확인 완료 (2026-09-05)** — 사업자등록번호 입력 → 사용자정보서비스로 업체명·소재지 자동 채움(실제 "(주)크리에이텍" 조회 확인). **기업구분·업종은 이 API에 필드 자체가 없어 자동 채움 불가로 확인됨** — MYC-002 수동 입력으로만 채운다 | 3 |
| MYC-002 | ~~업체 정보 수동 편집~~ | **구현·확인 완료** — 업체명/사업자등록번호/본점소재지/기업구분/주요업종, 입력 즉시 로컬 자동 저장(localStorage `myCompany`) | 3 |
| MYC-003 | ~~보유자격 등록~~ | **구현·확인 완료** — 자유 입력 추가형 목록 | 3 |
| MYC-004 | ~~수행실적 등록~~ | **구현·확인 완료** — 직접 입력 목록 | 3 |
| MYC-005 | ~~관심분야 태그~~ | **구현 완료** — 벤치마크의 IT/SW 특화 태그 그대로 사용(일반화는 보류) | 3 |
| MYC-006 | ~~업체정보 완성도 표시~~ | **구현·확인 완료** — 필드 채움 비율(%) 진행바, 사업자번호 자동입력 시 40%로 반영 확인 | 3 |
| MYC-007 | ~~공고 적합도 판정~~ | **구현·확인 완료 (2026-09-07)** — 등록된 내 업체 정보(지역·업종제한 등)와 조회 결과의 공고 조건(`indstrytyLmtYn`·`rgnLmtBidLocplcJdgmBssNm` 등, 정규화 모델에 `eligibility`로 추가됨)을 대조해 "제한 있음/제한 없어 보임/정보 없음" 배지 + 사유 목록 표시. 내 업체가 "대기업"이면 DTL-009 판정도 합산 확인. **실데이터로 검증 완료** — 통합검색 실 조회 결과 중 `indstrytyLmtYn: 'Y'`인 공고("제한 있음" + 사유 문구)와 `'N'`인 공고("제한 없어 보임")를 각각 열어 판정이 정확히 갈리는 것 확인. 정밀 업종코드 매칭은 아니고 Y/N 플래그·소재지 문자열 비교 수준의 참고용 판정. 최종 확인은 원문에서 하라는 면책 문구 포함 | 3 |

> **근거**: MYC-001은 T-5에서 이미 실측 완료한 사용자정보서비스 `getPrcrmntCorpBasicInfo02`(`inqryDiv=3` + `bizno`, 31일 이내 날짜범위 필수)를 그대로 재사용 — **CMP-002(경쟁사 업체명 매칭)가 아니라 바로 이 화면이 그 API의 원래 용도였음**. 새 API 활용신청 불필요, 2단계 엔진과도 무관해 3단계 배치.
> **DTL-009와의 관계**: MYC-007은 API 구조화 필드 기반 판정(3단계, 정확도 높음)이고 DTL-009는 첨부파일 텍스트 기반 판정(1단계, 키워드 매칭이라 정확도 낮음)이다. 둘은 서로 다른 신뢰도의 판정이라 완전히 대체 관계가 아니라 **보완 관계** — 1단계에서는 DTL-009 단독으로, 3단계부터 두 판정을 함께 보여준다.

### 3.9 SC-12 쇼핑몰 상품검색 (신규, 벤치마크 기능 이식)

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| MALL-001 | ~~검색 대상 전환~~ | **구현·확인 완료** — 품명 / 물품식별번호 중 선택 | 3 |
| MALL-002 | ~~품명 자동완성~~ | **구현·확인 완료 (2026-09-07)** — 새 API 없이 기존 검색 API로 구현. 2글자 이상 입력 시 300ms 디바운스 후 `numOfRows=8`로 미리 조회해 일치하는 품명(중복 제거) 최대 8개를 드롭다운으로 보여주고, 클릭하면 입력창에 채워짐. 실데이터로 확인 완료 | 3 |
| MALL-003 | ~~상품 조회~~ | **구현·확인 완료** — "노트북컴퓨터" 검색 시 167건 정상 반환 | 3 |
| MALL-004 | ~~결과 요약~~ | **구현·확인 완료** — 총 건수·판매업체 수·가격대(평균단가는 미구현) | 3 |
| MALL-005 | ~~정렬~~ | **구현 완료** — 계약단가 낮은/높은순, 업체명순, 계약종료 빠른순 | 3 |
| MALL-006 | ~~필터 — 업체~~ | **구현·확인 완료** — 판매업체별 건수 표시 후 선택 필터 | 3 |
| MALL-007 | 필터 — 지역 | **부분 구현** — "본사 소재지"가 아니라 "원산지" 기준으로 구현(공급지역/본사 소재지 필드가 응답에 별도로 없어 원산지로 대체) | 3 |
| MALL-008 | ~~필터 — 계약단가 범위~~ | **구현·확인 완료 (2026-09-07)** — 하한/상한 입력, 실데이터로 검증 중 **실제 버그 발견·수정**: 결과 리스트의 React `key`로 `물품식별번호`(`prdctIdntNo`)만 썼는데, 실데이터에 동일 물품식별번호를 서로 다른 업체·계약이 중복 사용하는 사례가 있어(예: 물품식별번호 `22810400`을 업체7·업체8이 각각 다른 계약으로 사용) 키 충돌이 발생 — 두 계약 모두 가격 필터에 안 맞는데도 필터링 후 목록에 한 건이 유령처럼 계속 남는 현상으로 나타남. `key`를 `물품식별번호-업체명-계약시작일` 조합으로 바꿔 해결(`MallWorkspace.jsx`). 수정 후 하한·상한 각각 실 데이터로 재검증 완료 | 3 |
| MALL-009 | ~~필터 — 인증마크~~ | **구현 완료** — 상품 카드에 인증마크 뱃지 표시(별도 필터 UI는 미구현) | 3 |
| MALL-010 | ~~상품 카드~~ | **구현·확인 완료** — 물품식별번호·제조사·원산지·공급지역·단가·계약기간·납품기한·기업구분·인증마크 | 3 |
| MALL-011 | ~~더 보기~~ | **구현·확인 완료 (2026-09-07)** — numOfRows=100 단건 조회 뒤 "더 보기" 클릭 시 pageNo를 증가시켜 다음 100건을 이어붙임. 2,400건 결과에서 실제로 100→200건씩 누적되는 것 확인 | 3 |

> **확인 완료 (2026-09-05)**: 7번째 API "조달청_종합쇼핑몰 품목정보 서비스"(`ShoppingMallPrdctInfoService`) 활용신청·실측 완료. End Point `https://apis.data.go.kr/1230000/at/ShoppingMallPrdctInfoService`, MALL-003~011에 쓸 오퍼레이션은 **`getMASCntrctPrdctInfoList`(다수공급자계약 품목정보)** — 파라미터 `prdctClsfcNoNm`(품명)으로 실제 필터링 확인(예: "노트북컴퓨터" 입력 → 정확히 해당 품목만 반환). 주요 응답 필드: `prdctIdntNo`(물품식별번호) · `cntrctCorpNm`(업체명) · `entrprsDivNm`(기업구분) · `cntrctPrceAmt`(계약단가) · `prdctOrgplceNm`(원산지) · `splyJrsdctRgnNm`(공급지역) · `cntrctBgnDate`/`cntrctEndDate`(계약기간) · `dlvrTmlmtDaynum`(납품기한) · `prodctCertList`(인증마크 목록) · `specDocAtchFileNmUrl1~5`(규격서 URL). 제3자단가계약은 같은 서비스의 `getThptyUcntrctPrdctInfoList`로 동일 패턴 조회. 이 화면도 1단계와 무관하게 바로 착수 가능.

### 3.10 SC-13 대시보드 (신규, 2026-09-08 추가) — 앱 메인 화면

| 기능ID | 기능명 | 상세 | 단계 |
|---|---|---|---|
| DASH-001 | ~~등록 현황 요약~~ | **구현·확인 완료** — 관심 키워드·경쟁사 등록 수, 내 업체 등록 여부를 카드로 요약, 클릭 시 해당 화면으로 이동 | 5 |
| DASH-002 | ~~저장내역 마감 임박~~ | **구현·확인 완료** — 로컬 저장내역(savedItems)에서 마감 임박(D-7 이내) 공고만 추려 즉시 표시 — API 호출 없이 로컬 데이터만 사용 | 5 |
| DASH-003 | ~~관심 키워드 새 공고 확인~~ | **구현·확인 완료** — "지금 확인" 버튼을 눌러야만 실행(자동 아님). 등록 키워드 중 최대 3개를 최근 31일·빠른조회로 병렬 검색해 마감일순으로 표시 | 5 |
| DASH-004 | ~~메뉴 카드~~ | **구현·확인 완료 (2026-09-08 추가)** — 쇼핑몰 상품검색·저장내역·평가분석 3개 화면으로 가는 카드. 관심 키워드·경쟁사는 등록 현황 카드가 그 역할을 겸함(위 DASH-001) | 5 |

> **왜 대시보드를 만들었나**: 벤치마크(nara-search.vercel.app)의 "브리핑" 기능을 참고해 도입 — 사용자가 매일 들어와서 한눈에 상태를 보는 화면이 필요하다는 판단. 화면 이름은 "브리핑"이 아니라 "대시보드"로 확정.
> **API 호출 폭증 방지가 핵심 설계 원칙**: 앱을 열 때마다 자동으로 정부 API를 호출하기 쉬운데, 사용자가 늘어날수록(예상 ~50명) 하루 호출 한도(1,000건)를 순식간에 소진할 위험이 있다. 그래서 로컬 데이터로 채울 수 있는 것(DASH-001·002)은 즉시 표시하고, 정부 API가 필요한 부분(DASH-003)은 반드시 사용자가 버튼을 눌러야만 실행되게 설계했다. 벤치마크 사이트도 실제로 브리핑을 "새로고침" 수동 버튼 방식으로 구현해 둔 것을 확인하고 같은 방향으로 확정했다.
> **메뉴 재배치 (2026-09-08 최종)**: 처음엔 대시보드를 기본 화면으로 삼고 나머지 전부를 흡수시켰으나, 같은 날 다시 논의해 **통합검색을 기본 화면으로 되돌리고, 대시보드는 관심 키워드·경쟁사·쇼핑몰·저장내역·평가분석 5개 화면만 메뉴 카드로 흡수**하도록 정리했다. 내 업체·알림·설정은 별도로 "내설정"이라는 대메뉴로 묶어(2장 네비게이션 참고) 최종적으로 **통합검색·대시보드·내설정 3개 대메뉴** 구조가 됐다. 모바일 "더보기" 시트는 이 과정에서 완전히 제거됨.

---

## 4. API 매핑

### 4.1 활용신청 대상 (8종, 전부 승인 완료)

| No | 서비스명 | 용도 | 매핑 기능 |
|---|---|---|---|
| 1 | 나라장터 입찰공고정보서비스 | 공고목록, 공고상세, 기초금액, 면허제한, 참가가능지역, 변경이력 | SRC-001, RES-001, DTL-001~003 |
| 2 | 나라장터 낙찰정보서비스 | 낙찰목록, 개찰결과 | SRC-001, RES-001, CMP-010 |
| 3 | 나라장터 사전규격정보서비스 | 사전규격 목록·상세, 규격서 파일 | SRC-001 |
| 4 | 나라장터 사용자정보(업체) | **사업자번호 → 업체정보 단건 조회**(정방향 이름검색 불가, T-5 참고) | MYC-001 (내 업체 자동입력) |
| 5 | 나라장터 계약정보서비스 | 계약체결정보, 계약변경이력 — **보류**(DTL-007·008 근거 참고, 단건 조회 미지원으로 구현 불가 판정) | DTL-007, DTL-008 |
| 6 | 공공조달통계정보서비스 | 기관·연도·업무구분별 집계 — **보류**(3.7절 근거, 어떤 연도로도 데이터 0건) | STA-001~004 |
| 7 | 조달청_종합쇼핑몰 품목정보 서비스(`ShoppingMallPrdctInfoService`) | 다수공급자계약·제3자단가계약 상품 목록(단가·계약기간·인증마크) | MALL-001~011 |
| 8 | 나라장터 발주계획현황서비스(`OrderPlanSttusService`, 2026-09-08 추가) | 기관별 연간 발주계획(사업명·예산·발주예정월·담당자) | SRC-019 발주계획 레이더 |

> **8번(발주계획현황서비스) 실측 메모 (2026-09-08)**: 별도 활용신청이 필요했다(기존 인증키로 최초 호출 시 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`, 신청 후 정상화). **파라미터명이 다른 7개 서비스와 다르다** — 낙찰·입찰공고·사전규격은 `inqryBgnDt`/`inqryEndDt`(Dt)인데 이 서비스는 `inqryBgnDate`/`inqryEndDate`(Date) — 실측 없이 기존 관례를 그대로 가정했다면 계속 `필수값 입력 에러`만 났을 자리. 오퍼레이션 접미사(Servc/Thng/Cnstwk/Frgcpt)는 기존 서비스와 동일하게 확인됨. 응답 필드 중 담당자 성명(`ofclNm`)·전화번호(`telNo`)는 마스킹 없이 원본이 내려오는데, 나라장터 공고상세 페이지 자체는 비로그인 사용자에게 "성+**"로 마스킹해서 보여주는 것과 비교해 우리 쪽에서 표시 전 직접 마스킹 처리함(전화번호는 아예 미표시).

> **CMP-002(경쟁사 업체명 매칭) 참고**: 4번 서비스로는 업체명→사업자번호 정방향 검색이 안 되므로, CMP-002는 별도 API 없이 **낙찰정보서비스 응답의 `bidwinnrNm`+`bidwinnrBizno` 쌍을 서버가 수집한 데이터 내에서 검색**하는 방식으로 구현한다(9장 T-5 참고).

- 개발계정: 자동승인, **일일 1,000건** (T-3 확인 완료, 1~3·5~7번 동일. 단 4번 사용자정보서비스만 **일일 10,000건**으로 확인 — 서비스별 상이)
- 운영계정: 활용사례 등록 시 트래픽 증량 신청 가능
- **인증키는 서버 환경변수로만 관리. 프론트엔드 노출 금지.**

**T-1·T-2·T-4·T-6·T-7 실측 방법**: 각 API 상세페이지 로그인 후 "활용정보 > 상세기능" 탭의 **미리보기(Preview)** 기능으로 코드 없이 즉시 확인 가능 (파라미터 입력 → 실제 응답 JSON 확인). 정확한 오퍼레이션명·엔드포인트도 이 탭에서 확인.

### 4.2 업무구분별 오퍼레이션 분기

입찰공고·낙찰·사전규격 3개 서비스 모두 업무구분(물품/용역/공사/외자)별로 오퍼레이션이 분리되어 있다.

| 업무구분 | 처리 |
|---|---|
| 용역 / 물품 / 공사 / 외자 | 해당 오퍼레이션 단일 호출 |
| 전체 | 4개 오퍼레이션 병렬 호출 → 병합 → 중복제거 |

"전체" 선택 시 호출량이 4배가 된다. 정밀 조회 + 전체 + 5년 조합이 최악의 케이스이므로 **호출 상한과 경고 노출을 반드시 설계할 것.**

> **실측 완료 (2026-09-05, 낙찰정보서비스 `ScsbidInfoService` 기준)**: 4개 오퍼레이션 전부 확인됨 — 용역 `getScsbidListSttusServc`, 물품 `getScsbidListSttusThng`, 공사 `getScsbidListSttusCnstwk`, 외자 `getScsbidListSttusFrgcpt`. 입찰공고·사전규격 서비스도 동일한 접미사 규칙(Servc/Thng/Cnstwk/Frgcpt)을 따를 가능성이 높지만, **BASE URL 자체가 서비스마다 다르므로**(낙찰정보서비스 End Point에 `BidPublicInfoService`·`CntrctInfoService` 등 다른 서비스명을 대입해 시도했으나 전부 `NO_OPENAPI_SERVICE_ERROR`) 확인 전까지 가정하지 말 것.

### 4.3 서버 라우트 설계

**구현 완료 (1단계, `functions/` — 2026-09-05)**

| 라우트 | 역할 |
|---|---|
| `GET /api/health` | API 연결 상태 확인 (SRC-018) — 낙찰정보서비스에 가벼운 실호출 1건을 던져 확인 |
| `GET /api/search/:kind` | `kind`=`bid`\|`award`\|`prespec`. 쿼리: `from,to`(YYYY-MM-DD)·`bizType`(전체/용역/물품/공사/외자)·`keywordType`·`keyword`. 5.2 Item으로 정규화·중복제거해 반환 |
| `POST /api/restriction-check` | **대기업 참여제한 자동판정(DTL-009·010)** — body `{ attachments:[{name,url}] }`, 클릭 시에만 프론트가 호출(온디맨드). PDF·HWP·HWPX 전부 이 라우트가 직접 처리(HWP는 `@ohah/hwpjs` 네이티브 파서, Cloud Run 불필요 — 2026-09-07 전환) |

> **설계 대비 실제 변경점**: 애초 안(`GET /api/detail/:bidNo`)은 폐기했다 — 나라장터 목록 API 응답 자체에 이미 DTL-001·003(진행정보·첨부목록)에 필요한 필드가 전부 포함되어 있어(실측으로 확인), 별도 상세 조회 API 호출이 필요 없다. 프론트가 목록에서 받은 Item을 그대로 상세 패널에 렌더링한다.

**3·4단계 착수 시 추가 예정 (미구현)**

| 라우트 | 역할 |
|---|---|
| `/api/company/lookup` | **업체명 → 사업자번호 검색**(CMP-002) — 외부 API 호출 없이 서버가 수집한 낙찰 데이터(`bidwinnrNm`+`bidwinnrBizno`) 내에서 자체 검색 (9장 T-5 참고) |
| `/api/company/mine` | **내 업체(MYC-001~007)** — 사업자번호 입력 시 사용자정보서비스(`getPrcrmntCorpBasicInfo02`, `inqryDiv=3`)로 자동입력 + 등록된 내 업체 정보 CRUD |
| `/api/contract/:bidNo` | 계약체결정보·변경이력 (DTL-007·008, 지연 로딩) |
| `/api/stats` | 시장동향 통계 (STA-001~004) |
| `/api/mall/search` | **쇼핑몰 상품검색(MALL-001~011)** — `getMASCntrctPrdctInfoList`/`getThptyUcntrctPrdctInfoList` 프록시 |
| ~~(Cloud Run) `/convert/hwp`~~ | **폐기 (2026-09-07)** — `@ohah/hwpjs`로 대체돼 별도 변환 라우트 불필요, `POST /api/restriction-check`가 HWP까지 직접 처리 |

---

## 5. 데이터 모델

### 5.1 서버 응답 (공통 래퍼)

```
SearchResponse {
  items: Item[]
  totalCount: number
  chunk: { done: number, total: number }   // 진행률
  elapsedMs: number
  mode: 'quick' | 'full'
  truncated: boolean                        // 빠른 조회로 잘렸는지
  range: { from: string, to: string }
}
```

### 5.2 Item (통합 정규화 모델)

3개 소스의 필드명이 서로 다르므로 **서버에서 단일 모델로 정규화**해 내려준다. 프론트는 소스 차이를 몰라야 한다.

```
Item {
  id: string                 // 공고번호 + 차수
  kind: 'bid' | 'award' | 'prespec'
  title: string              // 사업명
  bidNo: string              // 공고번호
  bidSeq: string             // 차수
  postedAt: datetime         // 공고일 / 등록일시
  orderOrg: string           // 발주기관
  demandOrg: string          // 수요기관
  bizType: string            // 업무구분
  region: string
  estimatedPrice: number|null   // 추정가격
  basePrice: number|null        // 기초금액
  bidDeadline: datetime|null    // 입찰마감
  proposalDeadline: datetime|null // 제안서마감
  openingAt: datetime|null      // 개찰일시
  // 낙찰 전용
  winner: { name: string, bizNo: string } | null
  awardAmount: number|null
  awardRate: number|null        // 낙찰률(%)
  isFailed: boolean             // 유찰·재입찰 — 입찰공고 ntceKindNm/reNtceYn 기반 (9장 T-7)
  noticeKind: string|null       // ntceKindNm 원문 그대로 (일반공고/변경공고/재공고/취소공고 등)
  sourceUrl: string             // 나라장터 원문
  attachments: { name: string, url: string }[]  // ntceSpecFileNm+ntceSpecDocUrl 쌍 (DTL-003·004)
  // 계약정보 (DTL-007·008, 상세 열람 시 지연 로딩 — 목록 API에는 없음)
  contract: {
    contractDate: datetime,
    contractAmount: number,
    contractor: { name: string, bizNo: string },
    changeHistory: { date: datetime, reason: string, amountBefore: number, amountAfter: number }[]
  } | null
  // 대기업 참여제한 판정 (DTL-009·010, "확인하기" 클릭 시에만 채워짐 — 목록 API에는 없음, 클라이언트가 온디맨드로 요청)
  restrictionCheck: {
    status: 'restricted' | 'clear' | 'needs_review'
    matchedFile: string|null      // 판정 근거가 된 첨부파일명
    excerpt: string|null          // 매칭된 문구 발췌
    checkedAt: datetime
  } | null
  // MYC-007용 원본 제한 필드 (입찰공고만 값 있음, 낙찰·사전규격은 null — 2026-09-06 추가)
  eligibility: {
    indstrytyLmtYn: string|null              // 업종제한여부
    rgnLmtBidLocplcJdgmBssNm: string|null     // 지역제한 기준
    cmmnSpldmdCorpRgnLmtYn: string|null       // 공동수급 지역제한여부
    prdctClsfcLmtYn: string|null              // 물품분류제한여부
  } | null
}
```

### 5.2-2 MyCompany (SC-11 내 업체, 로컬 저장)

```
MyCompany {
  bizNo: string
  corpNm: string
  hqRegion: string
  entrprsDiv: string        // 대기업/중견기업/중소기업/소기업/소상공인
  mainIndustry: string
  qualifications: string[]  // 보유자격 (자유 입력)
  performances: string[]    // 수행실적 (자유 입력)
  interestTags: string[]    // 관심분야
}
```

### 5.2-3 MallItem (SC-12 쇼핑몰 상품검색)

```
MallItem {
  prdctIdntNo: string        // 물품식별번호
  prdctSpecNm: string        // 규격명
  cntrctCorpNm: string       // 계약업체명
  entrprsDivNm: string       // 기업구분
  cntrctPrceAmt: number      // 계약단가
  prdctOrgplceNm: string     // 원산지
  splyJrsdctRgnNm: string    // 공급지역
  cntrctBgnDate: date
  cntrctEndDate: date
  dlvrTmlmtDaynum: number    // 납품기한(일)
  certList: string[]         // 인증마크 (prodctCertList 파싱)
  specDocUrls: string[]       // 규격서 URL
}
```

### 5.2-1 통계 응답 모델 (STA-001~004, SC-10 전용)

목록형 Item과 별개로 집계 전용 응답. 청크 분할 불필요(단건 호출).

```
StatSummary {
  scope: { orgName: string|null, year: number, bizType: string|null }
  totalAmount: number
  totalCount: number
  byBizType: { bizType: string, amount: number, count: number }[]   // STA-003
  byYear: { year: number, amount: number }[]                        // STA-002
}
```

### 5.3 로컬 저장 (localStorage)

| 키 | 내용 | 상한 |
|---|---|---|
| `recentQueries` | 최근 검색어 | 12 |
| `keywords` | 관심 키워드 | — |
| `competitors` | 경쟁사 (업체명 + 사업자번호) | — |
| `savedItems` | 저장 공고 | — |
| `settings` | 표시·알림 설정 | — |

4단계(알림) 진입 시에만 Firestore를 추가한다. 그 전까지는 **로그인 없이 동작**시켜 보안검토 부담을 낮춘다.

---

## 6. 공통 규칙

### 6.1 표기 규칙

| 항목 | 규칙 |
|---|---|
| 금액 | 천단위 콤마 + 억 환산 병기 (예: `4,602,080,000원 46억`) |
| 억 환산 | 소수 첫째자리까지 (`76.5억`) |
| 낙찰률 | **소수 둘째자리 고정** (`100.00` / `99.99`) |
| 날짜 | `YYYY-MM-DD` |
| 일시 | `YYYY-MM-DD HH:mm` (등록일시만 초 단위 허용) |
| 마감 임박 | `D-n` 뱃지. 당일 `D-DAY`, 경과 `마감` |
| 미제공 값 | `예산 미제공` 등 명시 (공란 금지) |

> 벤치마크 사이트는 낙찰률을 `100` / `99.998`로 자리수 불일치 노출. 우리는 **둘째자리 고정**으로 통일한다.

### 6.2 낙찰률 계산

> **T-4 실측 반영 (2026-09-05)**: 낙찰정보서비스가 `sucsfbidRate`를 이미 계산해서 제공하므로, **서버가 직접 계산하지 않고 API값을 그대로 포맷팅만** 한다. 아래는 API값이 없는 예외 상황에 한해서만 쓰는 폴백 공식이다.

```
1순위: API의 sucsfbidRate 값을 소수 둘째자리로 반올림해 그대로 사용
2순위(폴백, sucsfbidRate가 없을 때만): 낙찰금액 / 기준금액 × 100
  기준금액 우선순위: 기초금액 > 추정가격
  둘 다 없으면 '-' 표시 (0 또는 100으로 채우지 않는다)
```

### 6.3 상태 피드백

| 상황 | 처리 |
|---|---|
| 조회 진행 | 진행률(청크 done/total) + 경과 시간 실시간 표시 |
| 조회 완료 | 배너: `{유형} 완료 {건수}건, 조회 범위 {n}, {소요}초` |
| 빠른 조회 | 앰버 경고 배너 + 정밀 조회 유도 |
| 부분 실패 | 실패 청크 수 명시 후 부분 결과 표시 (전체 실패 처리 금지) |
| API 장애 | 상단 상태 표시 변경 + 재시도 안내 |
| 등록/삭제 | 우상단 토스트 |

**설계 원칙**: 데이터 누락 가능성을 숨기지 않는다. 진행률과 소요시간을 그대로 노출해 "느림"을 "사용자 선택"으로 전환한다. 이것이 벤치마크 사이트의 가장 잘 된 설계이며 반드시 계승한다.

### 6.4 디자인 토큰

7장(디자인 시스템) 참고. 다크 LG 마젠타 초안은 폐기되고 크림+클레이 팔레트로 대체됨.

---

## 7. 디자인 시스템

### 7.1 방향 설정

**주제**: 관공서 공고·장부 데이터. **사용자**: 시간 압박 속에서 숫자·마감일을 빠르게 스캔하는 입찰 실무자 1인.
**원칙**: 카드를 반복 배치하는 일반 SaaS 대시보드 룩을 피하고, 장부(ledger) 형태 — 가로줄 구분 + 상태를 왼쪽 컬러바로 표시 — 를 기본 레이아웃으로 삼는다. 화려함보다 "숫자가 눈에 바로 들어오는가"가 유일한 기준.

---

### 7.2 컬러 토큰

라이트(크림) 테마로 확정. 테일윈드 스타일 스케일(50~900) + 클로드 제품 색감(웜 크림 배경 + 클레이 포인트).

| 패밀리 | 50 | 100 | 400 | 600 | 800 |
|---|---|---|---|---|---|
| `cream` (배경) | `#FFFEFC` | `#FAF8F3` | `#DDD4C0` | `#A89878` | `#5F5342` |
| `clay` (브랜드/포인트) | `#FBF1EC` | `#F5DDD0` | `#CC785C` | `#9C4A31` | `#5E2B1B` |
| `ink` (텍스트/뉴트럴) | `#F7F6F3` | `#EDEBE5` | `#928A78` | `#504A3D` | `#26221C` |
| `sage` (낙찰/성공) | — | `#E4EEDD` | `#7FA968` | `#557A41` | `#35502A` |
| `amber` (마감임박/경고) | — | `#F6E8D2` | `#D9A44E` | `#B07E2E` | `#7A5A1E` |

**적용 매핑**

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg-app` | `cream-200 #F4F0E6` | 앱 배경 |
| `--bg-surface` | `cream-100 #FAF8F3` | 헤더바·필터바 등 구획 배경 |
| `--border-hairline` | `cream-400 #DDD4C0` | 행 구분선 |
| `--text-primary` | `ink-800 #26221C` | 본문 |
| `--text-secondary` | `ink-400 #928A78` | 기관명·부가정보 |
| `--brand-area` | `clay-400 #CC785C` | 버튼·로고 등 면적 큰 곳 |
| `--brand-text` | `clay-600 #9C4A31` | 활성 탭·강조 텍스트·링크 |
| `--status-success` | `sage-600 #557A41` (bg `sage-100`) | 낙찰 |
| `--status-warning` | `amber-600 #B07E2E` (bg `amber-100`) | 마감임박(D-day) |
| `--status-neutral` | `ink-300 #B8B2A3` | 비활성 아이콘·일반 상태 |

**규칙**: `clay-400`은 면적(버튼 배경)에만, `clay-600`은 텍스트·아이콘에만 쓴다. 라이트 배경이라 채도 높은 네온톤은 쓰지 않고 sage·amber 모두 톤을 낮춰 차분하게 유지한다.

---

### 7.3 타이포그래피

- 서체: **Pretendard** (한글 UI 표준, 탭라인 숫자 지원 좋음)
- 금액·낙찰률 등 숫자는 반드시 `font-variant-numeric: tabular-nums` — 자릿수 정렬로 비교가 쉬워야 함
- 스케일: 본문 13px / 부가정보 11px / 강조 숫자 14~15px / 화면 타이틀 18px
- 굵기 2단계만 사용: 400(본문) / 500(강조) — 700 이상 쓰지 않음

> **정정 (2026-09-07)**: 실제 배포 후 "전반적으로 글자가 작다"는 피드백을 받아 스케일을 한 단계씩 올렸다 — 본문 13→14px, 부가정보 11→12px(`text-[9~11px]` 임의값도 전부 +1px), 강조 숫자·화면 타이틀은 Tailwind `text-sm`(14→15px) 토큰 상향으로 함께 올라감. `web/src/index.css`의 `@theme`에서 `--text-xs`·`--text-sm`을 재정의하고 body 기본값을 14px로 바꿨다. 데스크톱 표·모바일 카드 모두 줄바꿈·잘림 없이 확인 완료.

---

### 7.4 레이아웃 — 장부(ledger) 컨셉

```
┌─────────────────────────────┐
│ 로고  조달검색      ● API 정상 │  ← 헤더바 (surface)
├─────────────────────────────┤
│▌사업명 (1줄, 말줄임)   [낙찰]  │
│▌기관 · 공고번호        46.0억 │  ← 왼쪽 컬러바 = 상태
│                    낙찰률100 │
├─────────────────────────────┤
│▌사업명                 [D-5] │
│▌기관 · 공고번호      08.31마감│
├─────────────────────────────┤
│  검색 경쟁사 키워드 저장 더보기│  ← 하단 5탭 (모바일, 2026-09-07 확정 — 아래 정정 참고)
└─────────────────────────────┘
```

- 카드+그림자 반복 금지. 행은 **가로줄(hairline)로만 구분**, 배경색 동일
- 왼쪽 3px 컬러바가 유일한 상태 표시 장치 — 뱃지 색과 통일 (초록=낙찰, 앰버=마감임박, 없음=일반 공고)
- 우측 정렬 영역(금액·뱃지)은 폭 고정, 좌측 텍스트는 `text-overflow: ellipsis`로 한 줄 유지
- 데스크톱은 4열 테이블(공고·사업명 / 업체·기관 / 금액·일정 / 핵심지표)로 확장, 모바일은 위 카드형 1열로 축약

> **구현 완료 (2026-09-07)**: PC용/모바일용을 "동시에" 만들어달라는 요청으로, 위 밀도 차이를 화면 전체에 일관되게 적용했다. 기법은 별도 라우트·파일·기기감지(`useMediaQuery` 등) 없이, **같은 컴포넌트 안에 데스크톱 블록과 모바일 블록을 나란히 두고 Tailwind `hidden md:flex`/`flex md:hidden`/`hidden lg:grid` 같은 반응형 표시 유틸리티로 토글**하는 방식 — `ResultRow.jsx`(모바일 카드 `lg:hidden` + 데스크톱 표 행 `hidden lg:grid`, `gridTemplate.js`의 `DESKTOP_GRID_COLS` 공유)에 이미 있던 패턴을 전 화면으로 확장했다. 대상: 통합검색(빈 상태 안내 카드 추가, 툴바 분리), 쇼핑몰(신규 `MallResultRow.jsx`+`mallGridTemplate.js`로 데스크톱 표 신설 — 이전엔 전 폭에서 카드 리스트뿐이었음), 경쟁사·내 업체·저장내역(데스크톱 폭 활용 재배치), 공고 상세(데스크톱 중앙 모달 / 모바일 풀스크린 바텀시트로 `DetailPanelBody` 공유 분리), 모바일 하단 네비게이션(위 정정 참고). 단순한 폼·짧은 리스트(관심 키워드 등)는 기존 한 트리 반응형 그대로 두고 이원화하지 않았다.
>
> 이 작업 직후 발견한 별개 버그도 같이 고쳤다: `flex` 컨테이너 안 `flex-1` 입력칸은 기본값(`min-width: auto`)상 플레이스홀더 텍스트보다 좁게 줄어들지 못해서, 옆에 고정폭 버튼/토글이 있으면 카드 테두리 밖으로 삐져나갔다(통합검색 좌측 360px 사이드바에서 실제 발견). `min-w-0`을 추가해 정상 축소되게 함 — 같은 패턴을 쓰던 6곳(통합검색·내 업체·관심 키워드·경쟁사·조회결과 툴바) 전부 반영.

#### PC(정밀) vs 모바일(심플) 밀도 차이

| | PC | 모바일 |
|---|---|---|
| 네비 | 상단 7탭 캡슐, 전체 라벨 노출 | 하단 5탭, 아이콘+짧은 라벨 |
| 필터 | 유형 칩 4종 상시 노출 | 결과 화면 진입 후에만 노출 |
| 테이블 | 4열 (공고·업체·금액·지표) | 1열 카드, 필드 압축 (낙찰률 등 2차 정보 생략) |
| 요약 | 우측 사이드바 상시 노출 | 결과 상단 배너로 축약 |
| 폭 대응 | 좁은 창에서는 표 영역 가로 스크롤 허용 (필터·네비는 고정) | 해당 없음 — 항상 1열 |

---

### 7.5 컴포넌트 스펙

#### 상태 뱃지
- 배경: 상태색 15% 투명도, 텍스트: 상태색 그대로, radius 4px, padding 2px 6px, 10px/500
- 종류: `낙찰`(success) / `D-n`(warning, n≤7일 때만 노출·그 이상은 날짜만 표기) / `마감`(neutral) / `추천`(recommend, 아래 5-1 참고)

#### 리스트 행
- padding 12px 14px, 좌측 border 3px (상태색), `border-radius: 0` 필수 (한쪽 보더는 라운드 처리 금지)
- 하단 hairline 0.5px, 마지막 행은 생략

#### 버튼
- Primary: `--brand-area (clay-400)` 배경, 흰 텍스트, radius 10px, **정적** (애니메이션 없음) — 화면당 1개만 (조회하기, 카카오톡 연결 등 핵심 액션)
- Secondary: 투명 배경 + `--border-hairline` 테두리, `--text-primary` 텍스트

#### 비비드 포인트 — 추천 뱃지 (유일한 애니메이션 요소)

앱 전체에서 **`추천` 뱃지 하나만** 움직인다. 로고·버튼·다른 뱃지·리스트 행은 전부 정적. 관심 키워드에 일치하거나 특별히 중요한 공고에만 붙는 신호이므로, 장식이 아니라 "놓치지 말라"는 의미를 움직임으로 전달하는 것 — 이 원칙을 벗어나 다른 요소에 확대 적용하지 않는다.

| 항목 | 값 |
|---|---|
| 그라데이션 색상 (마젠타레드) | `#D6003D → #FF2D55 → #FF5C8D → #FF2D55 → #D6003D` |
| 텍스트 색 | `#FFF5F7` (흰색 계열, 채도 높은 배경 위 대비용) |
| 그라데이션 이동 | `background-size: 300% 300%`, 5s ease-in-out infinite, position 0%→100%→0% |
| 글로우 펄스 | box-shadow `rgba(214,0,61,0.4)` ↔ `rgba(255,45,85,0.55)`, 2.6s ease-in-out infinite |
| 빛 스윕 | 대각선 반투명 하이라이트가 뱃지를 가로질러 지나감, 2.8s ease-in-out infinite (`::after` 의사요소, `skewX(-20deg)`) |
| 적용 위치 | 리스트 행 우측 뱃지 자리. 해당 행은 상단에 우선 정렬 |
| 적용 대상 | 관심 키워드 매칭 공고, 향후 재공고 레이더(5단계) 예측 항목 |
| 제외 | 로고, 버튼, 낙찰/D-day/마감 뱃지, 리스트 행 배경·테두리 전부 정적 유지 |

목업 파일(`조달검색_추천강조_데모.html`)에 실제 동작 코드 있음 — Claude Code 구현 시 그대로 참조.

#### 금액·낙찰률 표기 (6.1과 동일 규칙)
- 금액: 천단위 콤마 + 억 환산 병기
- 낙찰률: 소수 둘째자리 고정, 기준금액 없으면 `-`

#### 네비게이션
- 모바일: 하단 고정 5탭(검색/경쟁사/키워드/저장내역/더보기), 아이콘 18px + 라벨 10px, 활성 탭만 `--brand-text (clay-600)`, 나머지 `--status-neutral`. "더보기" 탭 → 바텀시트(`MobileMoreSheet.jsx`)로 내 업체·쇼핑몰 상품검색·알림·설정·평가분석(준비중 뱃지) 진입(2장 정정 참고)
- 데스크톱: 상단 캡슐형 **9탭**(통합검색/결과/평가분석/내 업체/관심 키워드/경쟁사/쇼핑몰 상품검색/저장내역/알림·설정), 원본과 동일 구조 유지

---

### 7.6 확정 필요 사항

| 항목 | 비고 |
|---|---|
| 브랜드 컬러 | 개인 취향으로 확정한 크림+클레이 팔레트. 회사 브랜드 규정과 무관 — 승인 절차 없음 |
| Pretendard 라이선스 | OFL, 상업적 사용 무료 — 문제 없음 |
| 다크 모드 지원 여부 | 1차는 라이트(크림) 전용으로 확정. 요청 있으면 2차에 추가 |

---

### 7.7 참고

- 조회 결과 화면 목업 2종(PC 정밀 / 모바일 심플)을 채팅에서 시각 확인 완료 — 장부형 레이아웃, 상태 컬러바 방식, 크림+클레이 팔레트
- 추천 뱃지 비비드 포인트(마젠타레드 애니메이션)는 별도 HTML 데모로 확정 완료 (5-1 참고)
- 6.1(표기 규칙)과 정합하며, 6.4(디자인 토큰)는 본 장(7장)으로 대체됨

---

## 8. 단계별 개발 범위

| 단계 | 범위 | 산출물 | 난이도 |
|---|---|---|---|
| **1** | ~~검색·결과·필터·정렬·내보내기·상세첨부, 대기업 참여제한 자동판정(DTL-009·010)~~ | **핵심 기능 구현·확인 완료 (2026-09-05)** — HWP까지 포함해 PDF·HWP·HWPX 전부 자동 처리 (2026-09-07, 12장 참고) | 낮음 |
| **2** | ~~청크 분할 엔진 (빠른/정밀, 진행률, 중단, 중복제거, 캐시)~~ | **구현·확인 완료 (2026-09-05)** — 실측 중 "31일 고정"이 오류임을 발견해 "+1개월(달력)" 규칙으로 정정, 청크당 999건 상한 초과분은 정직하게 표시(T-10). 캐시(브라우저 재사용)는 미구현 | **중간 — 핵심** |
| **3** | ~~경쟁사·내 업체(SC-11)·쇼핑몰 상품검색(SC-12)·키워드·저장내역~~, ~~계약정보(DTL-007·008)~~ | **경쟁사·내 업체·쇼핑몰·키워드·저장내역 구현·확인 완료 (2026-09-06)**. 계약정보는 **보류(2026-09-07 실측 후 구현 불가 판정 — 위 DTL-007·008 근거 참고)** | 중간 |
| **4** | 알림 (키워드 신규공고, 낙찰 발표), ~~시장동향 통계(SC-10)~~ | 스케줄러 + Firestore. 통계 대시보드는 **보류(2026-09-07)** — API에 실데이터 없음, 3.7절 참고 | 중상 |
| **5** | ~~재공고 레이더~~, ~~발주계획 레이더~~, ~~대시보드~~, ~~데이터 백업~~, ~~PWA 설치~~, 평가분석 | **재공고·발주계획 레이더, 대시보드, 데이터 백업, PWA 설치 구현·확인 완료 (2026-09-08)**. 평가분석만 **보류 유지**(3.6절 근거 — 개인정보·이용약관 문제 이전에 평가위원 점수 자체가 공고마다 구조화된 위치에 없는 구조적 한계 확인) | 평가분석만 높음/보류, 나머지는 중간 |

**3단계는 계약정보(DTL-007·008)를 뺀 나머지가 이미 완료 시점에 실무 배포 가능**하다. 계약정보는 애초 "상세 열람 시 단건 호출"이 가능하리라 가정했으나 실측 결과 이 API가 단건 조회를 지원하지 않아 보류됐다(위 근거 참고). 통계 화면은 새 화면·차트가 필요해 4단계에 배치하되, 계약정보와 같은 종류의 "날짜범위 대량조회 + 대량 볼륨" 문제가 없는지 4단계 착수 전에 먼저 실측할 것. 5단계 중 평가분석만 별건으로 계속 보류.

> **1단계 리스크 메모 (2026-09-05 실측·결정 완료)**: DTL-009(대기업 참여제한 자동판정)는 실사용자 핵심 요구라 1단계에 그대로 유지. 최근 5일 입찰공고 30건 샘플 실측 결과 첨부파일 85개 중 PDF 41%·HWP 31%·HWPX 24%·기타 4% — HWP 계열이 절반에 가까워 PDF만 처리하는 안은 기각.
> **HWP 처리 방식 전환 (2026-09-07)**: 애초 Cloud Run(LibreOffice headless)으로 계획했으나 GCP 결제 문제로 보류 중이었음. `@ohah/hwpjs`(Rust 기반 네이티브 파서, napi-rs) npm 패키지로 서버리스 함수 안에서 바로 텍스트 추출이 가능함을 확인해 전환 — 외부 서비스·GCP 계정 불필요. 실제 나라장터 공고문 hwp 2건(제한없음·제한있음 각 1건)으로 정확도 검증 완료. Vercel 배포 시 `dist/index.js`를 동적 경로로 require해서 Vercel의 정적 파일 추적이 놓치는 문제가 있어 `vercel.json`의 `functions.includeFiles`로 리눅스용 네이티브 바이너리를 명시적으로 포함시켜야 했음(12장 참고).

---

## 9. 미확정 항목 (TBD)

3단계(API 실측)에서 반드시 확인한다. **추정으로 코드를 짜지 않는다.**

| No | 항목 | 확인 방법 | 영향 |
|---|---|---|---|
| T-1 | ~~청크 단위~~ | **정정 완료 (2026-09-05, 2단계 구현 중 재검증)** — 처음 "31일 고정 상한"으로 확인했던 것은 **부정확했다.** 실제 규칙은 **날짜 수가 아니라 "시작일 + 1개월(달력 기준, 말일 클램프)"**다 — 예: `1/31~2/28`·`3/31~4/30`(말일 기준 다음달 말일로 클램프, JS `Date.setMonth` 오버플로 방식과 다름)은 정상, 반면 `2023-02-01~2023-03-03`(31일, 고정 일수로는 상한 이내)은 **실패**했다. 고정 일수로 청크를 나누면 시작월에 따라 임의로 성공/실패가 갈리므로, 반드시 달력 월 연산(`functions/src/lib/chunk.js`의 `addMonthsClamped`)으로 청크를 만들어야 한다 | 2단계 엔진 전체 |
| T-2 | ~~페이지당 최대 건수(numOfRows 상한)~~ | **확인 완료** — **999까지 정상**(응답 건수 일치), **1000 이상은 에러 없이 numOfRows가 조용히 기본값(10)으로 리셋됨.** 에러가 안 나므로 응답 바디의 `numOfRows` 필드를 매 호출 검증 필수 | 페이징 설계 |
| T-3 | ~~일일 트래픽 한도~~ | **확인 완료** — 개발계정 일일 1,000건(사용자정보서비스는 화면상 10,000건으로 확인 — 서비스별 상이) | 정밀 조회 상한 |
| T-4 | ~~낙찰결과·입찰공고에 기초금액/추정가격 포함 여부~~ | **확인 완료** — ① 낙찰정보서비스(`getScsbidListSttusServc`)는 기초금액 필드가 없는 대신 `sucsfbidRate`(낙찰률, 예 `95.454`)를 **API가 직접 계산해서 제공** → 6.2 낙찰률 계산 로직은 "서버 재계산"이 아니라 "API값을 소수 둘째자리로 포맷"으로 변경. ② 입찰공고정보서비스(`getBidPblancListInfoServc`) 목록 응답에는 **`asignBdgtAmt`(배정예산액)·`presmptPrce`(추정가격)가 이미 포함**됨. **기초금액(`bssamt`)은 별도 오퍼레이션**(`getBidPblancListInfoThngBsisAmount`=물품, `getBidPblancListInfoCnstwkBsisAmount`=공사)으로 조회해야 함 — 용역·외자용 기초금액조회 오퍼레이션 존재 여부는 화면 캡처에서 확인 안 됨(잘림), 필요 시 포털에서 스크롤해 확인 | 낙찰률 계산 로직, 5.2 데이터모델 |
| T-5 | ~~업체명→사업자번호 매칭 API 정확 명칭~~ | **확인 완료 — 단, CMP-002 설계 재검토 필요.** `getPrcrmntCorpBasicInfo02`(사용자정보서비스)는 `inqryDiv` 값에 따라 동작이 다름: `inqryDiv=1`(등록일시 기준)·`2`(변경일시 기준)는 **날짜범위 안의 전체 변경 업체를 그냥 나열**하며 `bizno`/`corpNm`을 넣어도 **완전히 무시됨**(엉뚱한 값을 넣어도 결과 동일 — 실제로 검증함). `inqryDiv=3`은 **`bizno` 필수 입력 + 정확히 일치하는 사업자번호 1건 조회**(정상 동작 확인, 예: bizno=6218138582 → "(주)크리에이텍" 정확히 반환)이며 `corpNm`만으로는 "필수값 입력 에러"(resultCode=08). **즉 이 API는 사업자번호→업체정보 역방향 조회만 가능하고, 업체명으로 사업자번호를 찾는 정방향 검색 기능 자체가 없음.** → **CMP-002("업체명 입력 → 후보 칩 목록") 재설계 필요**: 자체 API로는 이름 검색이 안 되므로, 낙찰정보서비스 응답에 이미 포함된 `bidwinnrNm`(업체명)+`bidwinnrBizno`(사업자번호) 쌍을 서버가 수집된 낙찰 데이터 내에서 자체적으로 검색해 후보를 만드는 방식으로 변경하거나(이미 조회된 캐시 데이터 내 검색), 별도 사전 인덱싱이 필요 | CMP-002 구현 — **설계 변경 필요** |
| T-6 | ~~첨부파일 URL 직접 접근 가능 여부~~ | **확인 완료** — 사전규격정보서비스(`getPublicPrcureThngInfoThng`) 응답의 `specDocFileUrl1~5` 필드로 실제 다운로드 URL 확인, 그 URL을 인증 없이 GET 요청 → **HTTP 200, `content-type: application/pdf` 정상 수신.** 별도 인증·세션 불필요 | DTL-005 |
| T-7 | ~~유찰·재입찰 구분 필드~~ | **확인 완료** — 낙찰정보서비스 응답에는 없음(낙찰 건만 내려오는 목록이라 구조상 당연). **입찰공고정보서비스(`getBidPblancListInfoServc`) 응답에 관련 필드 다수 존재**: `ntceKindNm`(공고종류: 일반공고/변경공고/재공고/**취소공고** 등), `reNtceYn`(재공고여부 Y/N), `rbidPermsnYn`(재입찰허용여부), `chgNtceRsn`(변경·취소 사유 텍스트), `befBidBbancNo`(이전 공고번호, 재공고 추적용 — SRC-010 재공고 레이더에도 활용 가능). RES-005 "유찰·재입찰만" 필터는 `ntceKindNm` 값 매핑으로 구현. 단, "유찰"이라는 값이 코드상 "취소공고"와 동일 개념인지, 전체 코드값 목록은 포털 참고문서(`.docx`)로 재확인 권장 | RES-005 |
| T-8 | 카카오 액세스 토큰 만료 주기·리프레시 정책 | 카카오 개발자 문서 실측 | SET-004 토큰 저장소 설계 |
| T-9 | 사용자별 토큰 저장 방식 (DB vs Firestore) | 알림 4단계 착수 시 결정 | SET-004 |

> **End Point 확정 목록 (2026-09-05, 포털 화면 캡처 기준)**
> - 낙찰정보서비스: `https://apis.data.go.kr/1230000/as/ScsbidInfoService`
> - 입찰공고정보서비스: `https://apis.data.go.kr/1230000/ad/BidPublicInfoService`
> - 사전규격정보서비스: `https://apis.data.go.kr/1230000/ao/HrcspSsstndrdInfoService`
> - 사용자정보서비스: `https://apis.data.go.kr/1230000/ao/UsrInfoService02`
> - 계약정보서비스: `https://apis.data.go.kr/1230000/ao/CntrctInfoService`
> - 공공조달통계정보서비스: `https://apis.data.go.kr/1230000/at/PubPrcrmntStatInfoService`
>
> 서비스마다 `/as/`, `/ad/`, `/ao/`, `/at/` 등 접두 경로가 다르므로 절대 추측하지 말고 위 목록을 그대로 사용할 것.
>
> **T-1 관련 메모(참고, 해소됨)**: 벤치마크 사이트 화면상 5년 조회 시 `264개`, 3개월 조회 시 `20`으로 표시됐던 것은 청크 수가 아니라 총 호출 수(페이징 포함)였을 가능성이 높음 — 청크 단위 자체는 "시작일+1개월"로 확정.

> ### ⚠️ T-10 (신규): "정밀 조회 = 전량 수집"은 일일 트래픽 한도 안에서 물리적으로 불가능하다 (2026-09-05 발견)
>
> 2단계 구현 중 청크당 실제 건수를 실측한 결과, **입찰공고 "용역" 업무구분 하나만으로도 달력 1개월에 12,000~41,000건**이 나온다(전국 단위라 학교 급식·청소 같은 소액 건까지 전부 잡힘). numOfRows 상한은 999건(T-2)이므로 한 청크를 "전부" 가져오려면 청크 하나당 수십 콜이 필요하고, 5년 정밀조회(업무구분 "전체" 기준)는 수천~수만 콜이 필요하다 — **개발계정 일일 1,000건 한도(T-3)로는 어떤 방식으로도 "완전 수집"이 불가능**하다.
>
> **대응 (2026-09-05 결정)**: 페이지네이션으로 "완전하게 만들려는" 시도를 포기하고, **청크당 정확히 1콜(최대 999건)만 가져오되, API가 알려주는 실제 총건수(`totalCount`)와 실제로 가져온 건수(`fetchedCount`)의 차이를 항상 사용자에게 그대로 노출**하는 방식을 택했다(6.3 원칙: 누락 가능성을 숨기지 않는다의 연장). 즉 "정밀 조회"의 의미를 "100% 완전 수집"에서 "선택한 전체 기간을 훑되, 각 구간의 상한(999건)까지만 보여주고 초과분은 정직하게 표시"로 재정의한다.
>
> **문서 전반에서 "정밀 조회 = 완전 정확"이라는 표현은 이 재정의로 대체된 것으로 읽을 것** (1.4 용어, 8장 2단계 완료조건 등).
>
> **실사용 사례로 재확인 (2026-09-08)**: 사용자가 "노래자랑" 키워드로 3년 조회했는데 실제로 존재하는 낙찰 건("2026 KBS전국 노래자랑 강릉시편")이 하나도 안 나오는 문제를 리포트 — 원인 추적 결과 T-10이 실제로 발생한 사례였다. 개찰일 2026-08-27인 이 건이 포함된 18일짜리 구간만 해도 "용역" 업무구분 하나에 낙찰 4,573건이 있었는데, 우리 시스템은 그중 첫 999건만 가져오고 나머지는 확인하지 않아 이 건이 누락됐다(정부 API가 관련도순이 아니라 등록순으로 내려주는 것으로 추정). 벤치마크 사이트(nara-search.vercel.app)는 같은 키워드로 정상 검색됐는데, 아마도 매 요청마다 정부 API를 실시간 호출하는 우리 구조와 달리 **백그라운드로 전체 데이터를 미리 자체 DB에 색인해두고 그 DB에서 검색하는 방식**일 가능성이 높다 — 이건 지금 구조를 통째로 바꾸는 별도의 큰 작업이라 v1 범위에서는 보류, "구간당 999건 상한" 안내 문구로 정직하게 노출하는 현재 방식을 유지한다.

---

## 10. 체크리스트

**착수 전**
- [x] 공공데이터포털 API 6종 활용신청 (개발계정) — 2026-09-05 전부 승인
- [x] 인증키 발급 확인
- [x] T-1~T-7 전부 실측 완료 (2026-09-05) — TBD 항목 소진
- [x] 이 문서 v1.0 확정

**사용자 확인 필요 (Claude가 대신 못 함 — 포털 로그인 필요, TBD 아님·후속 단계 착수 전 필요)**

| 항목 | 필요한 것 |
|---|---|
| ~~계약정보서비스 실사용 전~~ | **해소 (2026-09-07)** — 포털 화면 캡처 대신 Claude가 API를 직접 실측함. 결과: DTL-007·008은 이 API의 구조적 한계로 보류 결정(위 근거 참고), 착수 안 함 |
| ~~공공조달통계정보서비스 실사용 전~~ | **해소 (2026-09-07)** — 사용자가 상세기능 탭 캡처, 오퍼레이션·파라미터(`getTotlPubPrcrmntSttus`/`srchBssYear`) 확인함. 결과: 어떤 연도로도 데이터 0건이라 SC-10 자체를 보류 결정, 착수 안 함 |
| CMP-002 설계 검토 | T-5 실측으로 "업체명 정방향 검색 API 없음"이 확인됨 — 3.2절에 제안한 "낙찰 데이터 내 자체 검색" 방식으로 갈지, 사용자와 방향 재확인 |

**1단계 완료 조건**
- [x] 3개 유형 모두 단일 기간 조회 성공 (2026-09-05, `web`+`functions` 로컬 실행으로 확인)
- [x] 결과 목록 렌더링 — 데스크톱 4열 테이블(공고·사업명/업체·기관/금액·일정/핵심지표) + 모바일 1열 카드, 반응형 전환까지 실제 확인 (2026-09-05)
- [x] CSV/Excel 내보내기 (실제 클릭해 콘솔 에러 없이 동작 확인, 2026-09-05)
- [x] 인증키가 브라우저 네트워크 탭에 노출되지 않음 확인 (2026-09-05 — 네트워크 탭에 `/api/...` 자체 서버 요청만 존재, `serviceKey` 파라미터·`apis.data.go.kr` 직접 호출 없음)
- [x] ~~Cloud Run(LibreOffice headless) 컨테이너 배포~~ — **불필요해짐 (2026-09-07)**. `@ohah/hwpjs` 네이티브 파서로 대체해 GCP 결제·별도 컨테이너 없이 해결. `services/hwp-convert/`는 이제 미사용 코드로 남음
- [x] DTL-009 "확인하기" 클릭 → 뱃지(제한있음/제한없음/확인필요) + 근거 문구 + 원문 링크 정상 표시 (실제 PDF·HWP·HWPX 파일로 확인)
- [x] DTL-009가 PDF·HWP·HWPX 전부 정상 처리 확인 (실제 나라장터 첨부파일로 테스트, HWP도 2026-09-07부로 완전 지원)

**2단계 완료 조건**
- [x] 5년 정밀 조회 완주(T-10 재정의 기준 — 전체 기간 청크 순회는 완주하되 청크당 999건 상한, 초과분은 정직하게 표시. 백엔드 잡 실행으로 확인, 2026-09-05)
- [x] ~~진행률·소요시간 실시간 표시~~ — **구현·확인 완료 (2026-09-07)**. Vercel 서버리스 전환(step 방식 재설계)에 맞춰 실 배포 사이트에서 8청크 정밀조회를 끝까지 완주시키며 "청크 X/Y · 경과초" 배너 확인
- [x] ~~조회 중단 정상 동작~~ — **구현·확인 완료 (2026-09-07)**. `/jobs/:jobId/cancel` 호출 후 상태가 `cancelled`로 바뀌고 이후 step 호출이 더 진행하지 않는 것 확인
- [ ] 중복제거 검증 (동일 공고번호+차수 단일화) — 코드상 로직은 있음(`searchJob.js`의 `seen` Set), 실제 중복 데이터로 검증은 아직 안 함
- [ ] 부분 실패 시 부분 결과 표시 — 코드상 로직은 있음(`errors` 배열 수집), 실제 부분 실패 상황 재현 검증은 아직 안 함

---

## 11. 첨부 자료

| 파일 | 내용 |
|---|---|
| `조달검색_비비드포인트_데모.html` | 초기 시안 — 로고·버튼에 그라데이션 (폐기, 참고용) |
| `조달검색_추천강조_데모.html` | **확정본** — 추천 뱃지만 마젠타레드로 반짝임, 실제 동작하는 CSS 애니메이션 코드 포함. 7.5 컴포넌트 스펙 구현 시 이 파일의 `<style>` 블록을 그대로 참조 |

---

## 12. Claude Code 시작 안내

### 진행 순서
1. 본 문서를 프로젝트 루트에 `SPEC.md`로 저장하고 읽게 한다
2. `조달검색_추천강조_데모.html`도 함께 전달 — 비비드 포인트 애니메이션의 실제 구현 참조용
3. **9장 TBD(T-1~T-9) 중 API 실측이 필요한 항목(T-1~T-7)을 먼저 스크립트로 실행** — 실제 응답 구조를 보기 전에는 8단계(청크 엔진) 코드를 작성하지 않는다
4. 실측 결과로 5장 데이터 모델을 검증·수정한 뒤 1단계 MVP 착수

### 인프라 (개인 프로젝트 기준)

> **정정 (2026-09-06)**: 아래 "Firebase Hosting + Cloud Functions + Cloud Run" 계획은 **폐기**됐다. 실제로 결제 계정을 연결하려는 단계에서, 기존 결제 계정이 종료된 상태라 새로 만들어야 했고 ₩40,000 일회성 선불 결제가 필요했다. 구글 문서상 이 선불금이 계정을 그냥 해지하면 환불이 보장되지 않는다는 점(Postpay 전환 후 해지해야만 환불)까지 확인되어, **결제 계정 없이 배포 가능한 Render.com으로 전환**했다. 투두캘린더는 Cloud Functions/Cloud Run을 아예 쓰지 않고 Firebase Hosting+Firestore(무료 Spark 플랜)만으로 동작하는 앱이라 애초에 결제가 필요 없었던 것 — "동일 방식 재사용"이 성립하지 않았다.

> **정정 (2026-09-07)**: 위 Render.com 계획도 실행 전에 **Vercel로 최종 변경**했다. 다른 사람들도 URL로 접속해 쓰게 할 계획이라 무료 티어의 콜드스타트·슬립보다 프론트+백엔드 같은 도메인 배포(별도 CORS 설정 불필요)와 Git 연동 자동 배포가 더 맞아서다. 다만 Vercel의 서버리스 함수는 **HTTP 응답을 보내는 즉시 프로세스가 얼려지고, 다음 요청이 같은 인스턴스로 간다는 보장도 없다** — 그래서 2단계 청크 엔진(SRC-006 등)이 쓰던 "백그라운드에서 계속 도는 in-memory Map" 방식이 그대로는 통하지 않는다. 이를 "HTTP 호출 1번 = 청크 1개 처리"로 재설계했다(`functions/src/lib/searchJob.js`의 `stepJob`, 프론트는 `done` 될 때까지 반복 호출) — Render였어도 원래 방식이 통했겠지만, 어느 서버리스 환경으로 가든 이식되는 더 견고한 구조라 그대로 유지한다. Job 상태 저장소도 `KV_REST_API_URL` 환경변수가 있으면 Vercel KV(여러 인스턴스가 공유), 없으면(로컬 개발) 메모리 Map을 쓰도록 분기했다(`functions/src/lib/jobStore.js`).

- **Vercel** — 저장소 루트의 `api/index.js`(백엔드 Express 앱을 감싼 서버리스 함수)와 `web/`(Vite 빌드, 정적 배포)을 한 프로젝트로 배포. `vercel.json`이 빌드·라우팅 정의 (카드 등록 불필요, 무료 Hobby 플랜)
- 백엔드(Express, `functions/`)가 나라장터 API 프록시 역할 (인증키는 반드시 서버 환경변수, 프론트 노출 금지 — 4.1 참고)
- **HWP 자동변환 — Cloud Run 없이 해결 (2026-09-07)**: 애초 결제 계정 문제로 보류했던 것을, `@ohah/hwpjs`(Rust 기반 네이티브 파서, napi-rs) npm 패키지로 대체 — 서버리스 함수 안에서 바로 처리돼 GCP 계정·별도 컨테이너가 필요 없다. `services/hwp-convert/`는 이제 미사용. 단, Vercel의 정적 파일 추적(`@vercel/nft`)이 `dist/index.js`를 절대경로로 동적 require하는 코드는 못 따라가서, `vercel.json`의 `functions["api/index.js"].includeFiles`로 리눅스용 네이티브 바이너리(`dist/index.js`, `dist/hwpjs.linux-x64-gnu.node`)를 명시적으로 포함시켜야 실제 배포본에서 동작했다(처음엔 로컬에선 되는데 배포본에서만 `Cannot find module`로 실패해서 원인 특정에 시간이 좀 걸림).
- 2단계 청크 조회 진행상태는 Vercel KV(Redis 호환, `@vercel/kv`)에 저장 — Vercel 대시보드에서 프로젝트에 KV 스토어를 연결하면 `KV_REST_API_URL`/토큰 환경변수가 자동 주입된다. 미연결 시(로컬 개발) 메모리 Map으로 자동 폴백. `@vercel/kv` 패키지 자체는 deprecated 표시가 뜨지만(설치 시 "Upstash Redis로 이전됨" 안내) 여전히 동작함 — Vercel이 KV 스토어를 새로 만들 때 같은 환경변수 이름을 유지해주는지는 실제 연결 시점에 확인 필요, 안 맞으면 `@upstash/redis`로 교체
- 4단계 알림(KWD-003·RES-018) 데이터도 같은 KV 인스턴스를 키 프리픽스만 다르게(`kakao:*`) 재사용 — 새 DB 추가 없음. 하루 1회 배치는 Vercel Cron(`vercel.json`의 `crons`, `/api/cron/run-alerts`)으로 실행 — Hobby 플랜은 크론 주기가 하루 1회로 제한되고 정확한 실행 시각도 지정한 시간대 내에서만 보장된다. 추가 환경변수 필요: `KAKAO_CLIENT_ID`·`KAKAO_CLIENT_SECRET`·`KAKAO_REDIRECT_URI`(카카오 개발자 콘솔에서 발급)·`SESSION_SECRET`(로그인 쿠키 서명용 무작위값)·`CRON_SECRET`(크론 라우트를 아무나 못 부르게 막는 무작위값 — Vercel이 크론 호출 시 자동으로 `Authorization: Bearer` 헤더에 실어줌)
- DB 없음(잡 상태 저장용 KV 제외) — 1~3단계 사용자 데이터는 localStorage만으로 동작 (5.3 참고)
- 로그인 없음 (4단계 카카오 연동 전까지)

### 절대 지켜야 할 것
- 5장 Item 정규화 모델을 프론트가 그대로 쓰게 만들 것 — 소스별 필드명 차이를 프론트가 알게 하지 않는다
- 6.3 상태 피드백 원칙(진행률·소요시간 숨기지 않기)은 타협하지 않는다
- 7.5 비비드 포인트는 추천 뱃지 하나에만 적용 — 다른 요소로 확대하지 않는다
- 핵심 파일(서버 프록시 라우트, 청크 분할 로직) 수정 시 변경 범위를 먼저 설명하고 진행한다

### 착수 전 마지막 확인
- [x] 공공데이터포털 API 6종 활용신청 완료, 인증키 확보
- [x] T-1~T-7 전부 실측 완료 (2026-09-05)
- [x] 디자인 방향(7장) 최종 확인 — 크림+클레이 팔레트, PC 가로스크롤, 추천 뱃지 비비드 포인트
